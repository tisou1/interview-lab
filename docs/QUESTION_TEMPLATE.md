# 新题模板

将以下内容复制到题库文档，替换编号、标题和元数据。编号示例 151；请先确认实际题库里未使用该编号（可参考 `tests/parser.test.ts` 的「编号完整性」断言）。不要把本模板标题一起复制成题目。

````md
## 151. 如何设计一个可取消的异步任务？

<!-- question: {"category":"javascript","type":"coding","difficulty":"intermediate","tags":["Promise","AbortController"],"estimatedMinutes":10} -->

描述题目要求，也可以提供待分析代码。

```js
async function task(signal) {
  // 写下你的实现思路
}
```

<details>
<summary>展开参考答案与解析</summary>

核心答案：先给出结论，20–60 秒可讲完。

### 原理与示例

说明运行机制，并给出最小可运行示例。

### 参考实现

```js
// 参考实现代码
```

### 边界与易错点

补充边界条件、反例或常见误区。

### 追问

进一步说明取舍与项目应用。

</details>

---
````

要点：

- `<summary>` 之后**必须留一个空行**，否则正文会被当成原始 HTML 而不做 Markdown 解析。
- 答案区用 Markdown，不要写 `<div>`、`<h3>` 之类的 HTML 标签。
- 代码块使用围栏写法，代码内部的空行是安全的。
- 小节标题只用固定词表：核心答案（无标题首段）、`原理与示例`、`边界与易错点`、`追问`、`参考实现`、`复杂度与取舍`、`设计框架`、`设计要点与边界`、`排查步骤`、`评分点`。
- 短题可省略部分小节，但必须保留核心答案，并至少含「边界与易错点」或「追问」之一。
- 答案区之后除分隔线与空行外不得有其他内容。
- 保存后运行 `pnpm content` 校验；新增/修改题目后可运行 `pnpm exec vitest run tests/parser.test.ts` 做结构回归。
