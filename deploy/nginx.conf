# Deploy dist/ to this directory and adapt server_name to the real host.
server {
    listen 80;
    server_name _;
    root /var/www/frontend-interview-lab/dist;
    index index.html;

    location /assets/ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
