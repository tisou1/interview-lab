# 前端面试题学习网站开发计划书

## 1. 项目概述

项目名称暂定为 **Frontend Interview Lab**。

这是一个面向个人求职复习的 React 前端面试训练网站。现有 115 道 JavaScript、React、浏览器、TypeScript、工程化和系统设计题将作为首批内容。

核心目标：

- 将长篇 Markdown 题库转化为适合主动回忆的交互式网站。
- 支持浏览、单题练习、错题复习和模拟面试。
- 保存个人学习进度，无需注册登录。
- 网站本身可作为 React 求职项目展示。
- 优先保证内容质量、交互效率和工程深度，不追求复杂后端。

成功标准：

- 115 道题全部可搜索、筛选和练习。
- 用户可以先回答，再分层查看题解。
- 收藏、草稿、熟练度和复习计划刷新后不会丢失。
- 能生成一场模拟面试及结果报告。
- 桌面端和移动端均可正常使用。
- 可进行静态部署，国内网络环境下能够稳定访问。

---

## 2. 项目定位与范围

### 首版包含

- 首页学习仪表盘
- 分类题库浏览
- 单题练习模式
- 模拟面试模式
- 收藏夹、错题本、待复习列表
- 本地回答草稿
- 熟练度自评
- 固定间隔复习计划
- 搜索、分类、难度、标签筛选
- 深色/浅色主题
- 学习进度统计
- 键盘快捷键
- 响应式移动端布局
- 当前学习路线与思维导图入口

### 首版不包含

- 用户注册和登录
- 服务端数据库
- 云端同步
- 在线执行 JavaScript
- React 在线编译预览
- AI 自动评分
- 评论、排行榜和社区功能
- 题目在线编辑后台
- 付费、会员和商业化功能

后续只有在个人版稳定使用后，才评估账号、云同步和在线代码执行。

---

## 3. 技术方案

### 技术栈

- React 19 + TypeScript
- Vite
- React Router，使用 `HashRouter` 兼容纯静态部署
- CSS Variables + CSS Modules
- Vitest
- React Testing Library
- Playwright
- pnpm

必要生产依赖：

- `react`、`react-dom`：应用运行基础。
- `react-router-dom`：管理题目详情、练习、模拟面试和报告路由。

题库转换工具只在构建阶段使用，作为开发依赖，不进入浏览器 bundle。

### 项目目录

```text
interview-lab/
├── scripts/
│   └── build-question-data.mjs
├── src/
│   ├── app/
│   ├── components/
│   ├── features/
│   │   ├── dashboard/
│   │   ├── questions/
│   │   ├── practice/
│   │   ├── mock-interview/
│   │   └── review/
│   ├── data/
│   ├── hooks/
│   ├── storage/
│   ├── styles/
│   └── types/
├── tests/
└── public/
```

### 内容转换

现有 `JavaScript_React原理与实战模拟面试题库.md` 作为内容源。

构建脚本负责：

1. 按一级标题识别题目分类。
2. 按 `## 数字. 标题` 识别题目。
3. 提取 `<details>` 前的题目和待分析代码。
4. 提取 `<details>` 内的答案 HTML。
5. 按答案中的三级标题拆分为：
   - 核心答案
   - 原理解析
   - 参考实现
   - 追问
   - 易错点
   - 评分点
6. 清理不必要的 HTML 属性并进行白名单过滤。
7. 生成只读的结构化题目数据。

首版不在浏览器运行时解析十万字 Markdown。

---

## 4. 核心数据接口

```ts
type QuestionCategory =
  | 'javascript'
  | 'react'
  | 'browser'
  | 'typescript'
  | 'engineering'
  | 'coding'
  | 'architecture'

type QuestionType = 'theory' | 'coding' | 'debugging' | 'scenario'

type Difficulty = 'basic' | 'intermediate' | 'advanced'

interface AnswerSection {
  id: string
  title: string
  html: string
}

interface Question {
  id: number
  slug: string
  title: string
  category: QuestionCategory
  type: QuestionType
  difficulty: Difficulty
  tags: string[]
  estimatedMinutes: number
  promptHtml: string
  answerSections: AnswerSection[]
}
```

学习进度：

```ts
type Mastery = 'new' | 'again' | 'hard' | 'good'

interface QuestionProgress {
  questionId: number
  mastery: Mastery
  favorite: boolean
  attempts: number
  goodStreak: number
  lastReviewedAt?: string
  nextReviewAt?: string
  totalSeconds: number
  draft: string
}

interface PracticeSession {
  id: string
  mode: 'practice' | 'mock'
  questionIds: number[]
  currentIndex: number
  startedAt: string
  completedAt?: string
  flaggedQuestionIds: number[]
  revealedQuestionIds: number[]
}
```

本地存储：

```text
frontend-interview-lab:v1
```

存储内容包含：

- 数据版本号
- 各题学习进度
- 当前未完成练习
- 最近模拟面试
- 用户筛选偏好
- 主题设置

解析失败时保留原始数据备份并恢复为空状态，不让页面白屏。

---

## 5. 页面与路由

```text
#/                         首页仪表盘
#/questions                题库浏览
#/questions/:id            题目详情
#/practice                 练习配置
#/practice/session         单题练习
#/review                   错题与待复习
#/mock                     模拟面试配置
#/mock/session             模拟面试进行中
#/mock/report/:sessionId   模拟面试报告
#/roadmap                  学习路线与思维导图
```

### 首页

展示：

- 今日待复习数量
- 总体掌握进度
- JavaScript、React 等分类掌握率
- 最近学习记录
- 继续上次练习
- 开始随机练习
- 开始模拟面试

### 题库浏览

支持：

- 关键词搜索
- 分类、题型、难度、标签筛选
- 只看收藏、错题、未学习
- 按编号、难度、最近复习时间排序
- 列表与紧凑视图切换
- URL 保存筛选条件

题目卡片只展示标题、标签、难度、熟练度和预计时间，答案默认不展开。

---

## 6. 题目交互设计

### 浏览模式

点击题目进入详情页。

详情页顺序：

1. 题目
2. 待分析代码
3. “开始思考”计时器
4. 本地回答草稿
5. “查看参考答案”
6. 分层答案
7. 自评按钮
8. 上一题/下一题

答案分层展示：

```text
一句话结论
原理解析
代码示例
易错点
面试追问
项目应用
```

第一次点击只展开核心答案，其余部分独立折叠，避免一次出现大段文字。

### 单题练习模式

```text
显示题目
→ 开始计时
→ 用户口述或填写提纲
→ 查看参考答案
→ 对照各层解析
→ 自评
→ 自动保存
→ 下一题
```

采用软限制：

- 不强制填写答案。
- 用户可随时展开解析。
- 展开前提示当前思考时间。
- 展开后必须选择熟练度才能进入下一题，也可明确选择“跳过”。

自评快捷键：

- `1`：不会
- `2`：模糊
- `3`：掌握
- `Space`：展开核心答案
- `←/→`：上一题/下一题
- `F`：收藏
- `R`：待复查

输入框聚焦时禁用全局快捷键，避免干扰输入。

### 复习间隔

首版使用固定规则：

| 自评                | 下次复习 |
| ------------------- | -------- |
| 不会                | 1 天后   |
| 模糊                | 3 天后   |
| 掌握，连续 1 次     | 7 天后   |
| 掌握，连续 2 次     | 14 天后  |
| 掌握，连续 3 次以上 | 30 天后  |

再次选择“不会”时清空连续掌握次数。

---

## 7. 模拟面试设计

### 面试配置

用户可选择：

- 分类
- 难度
- 题量：5、10、15、20
- 总时长
- 是否包含代码题
- 是否优先抽取错题
- 是否打乱顺序

题目抽取需避免同一场重复，并兼顾分类分布。

### 面试过程

- 一次只显示一道题。
- 不允许查看答案。
- 显示总剩余时间和当前题用时。
- 支持“已回答”“不会”“待复查”。
- 支持前后切题和答题卡。
- 刷新后恢复当前场次和计时状态。
- 时间结束自动交卷。

### 面试报告

展示：

- 总用时和平均每题用时
- 各分类题目数量
- 已回答、不会、待复查数量
- 用户逐题自评
- 本场薄弱分类
- 建议复习题目
- 统一查看完整题解
- 将不会的题加入复习队列

首版没有客观自动判分，报告明确使用“自评结果”，避免制造虚假分数。

---

## 8. 视觉与响应式设计

视觉方向采用“开发者知识库”：

- 浅色背景、深蓝主色，延续现有思维导图。
- JavaScript、React、工程化等分类使用固定辅助色。
- 正文强调长时间阅读体验。
- 代码块使用高对比等宽字体。
- 动效控制在 150–220ms，并支持 reduced motion。
- 深浅色主题均达到可读对比度。

桌面端：

- 左侧导航
- 中央题目区
- 右侧进度/目录辅助栏

移动端：

- 左侧导航改为抽屉
- 操作按钮固定在底部
- 答案单列展示
- 草稿框支持全屏编辑
- 不依赖 hover 完成交互

---

## 9. 中国使用环境专项

- 在中低端 Android 和 CPU throttling 下验证交互。
- 模拟 Slow 4G 和接口延迟。
- 不依赖境外字体、CDN 或运行时服务。
- 所有题库、样式和图标随静态构建部署。
- 不采集手机号、设备标识等个人信息。
- 本地学习数据仅保存在浏览器，并提供“一键导出、导入和清空”。
- 部署到中国境内服务器时，按实际服务商要求处理 ICP 备案。
- 同时准备 PNG 截图和演示视频，避免演示地址临时无法访问。

---

## 10. 工程质量

### 性能

- 首屏不加载全部答案 HTML。
- 题目按分类或路由分块加载。
- 搜索建立轻量预处理索引。
- 搜索输入做短防抖。
- 115 道题无需虚拟列表；题量达到 1,000 后再评估。
- 记录构建产物大小并设置合理 bundle budget。

### 安全

- 题库内容只在构建阶段转换。
- HTML 使用严格白名单过滤。
- 不接受用户输入 HTML。
- 不将草稿拼入 `innerHTML`。
- 外部链接使用安全属性。
- 不在前端存放任何 secret。

### 可访问性

- 所有操作可使用键盘完成。
- 折叠答案使用正确的 button 和 `aria-expanded`。
- 计时器不进行高频屏幕阅读器播报。
- 颜色不作为熟练度的唯一信息。
- 对话框具备焦点锁定和关闭后焦点恢复。

---

## 11. 测试计划

### 单元测试

- 题库解析和分类映射
- 搜索与筛选
- 随机抽题去重
- 复习时间计算
- 掌握连续次数更新
- localStorage 版本迁移
- 模拟面试计时恢复
- 数据导入校验

### 组件测试

- 题目答案默认隐藏
- 点击后分层展开
- 查看答案后完成自评
- 草稿自动保存与恢复
- 快捷键在输入框中不误触发
- 收藏、错题和筛选状态同步
- 模拟面试中答案不可见
- 报告正确生成复习列表

### E2E

1. 浏览题库 → 搜索 React → 打开题目 → 展开答案 → 标记模糊。
2. 开始 5 题练习 → 填写草稿 → 刷新 → 恢复场次。
3. 完成模拟面试 → 查看报告 → 错题进入复习队列。
4. 导出学习数据 → 清空 → 导入 → 恢复进度。
5. 移动端完成一道题和自评。
6. 深浅色主题刷新后保持。

### 验收标准

- 115 道题数量、编号、分类和答案均未丢失。
- 所有答案初始隐藏。
- 浏览器刷新不会丢失学习进度。
- 模拟面试进行中不能查看答案。
- 核心流程无键盘操作障碍。
- production build、typecheck、unit test 和 E2E 全部通过。

---

## 12. 实施阶段

### 阶段一：基础骨架

- 创建 React + TypeScript + Vite 工程。
- 建立路由、主题、布局和测试环境。
- 完成题库转换脚本与数据校验。
- 实现题库浏览和题目详情。

### 阶段二：学习闭环

- 实现草稿、自评、收藏和本地存储。
- 实现单题练习和复习调度。
- 完成首页统计和学习进度。
- 加入快捷键与移动端操作栏。

### 阶段三：模拟面试

- 实现面试配置、抽题和计时。
- 实现答题卡、刷新恢复和交卷。
- 实现自评报告与错题转复习。

### 阶段四：质量与交付

- 补齐 unit、component 和 E2E。
- 完成可访问性、性能和移动端检查。
- 增加学习数据导入/导出。
- 编写 README、架构说明、性能报告和 ADR。
- 构建静态版本并准备国内可访问的演示。

---

## 13. 最终交付物

```text
前端面试题学习网站/
├── 可运行网站源码
├── 115 道结构化题目
├── 自动内容转换脚本
├── 单题练习系统
├── 复习与进度系统
├── 模拟面试与报告
├── 自动化测试
└── docs/
    ├── ARCHITECTURE.md
    ├── CONTENT_PIPELINE.md
    ├── TESTING.md
    ├── PERFORMANCE.md
    └── decisions/
```

配套保留：

- 12 周前端学习路线
- JavaScript / React 原理题库
- 前端技术学习路线思维导图

## 14. 已确定的默认决策

- 网站定位为个人学习工具，同时具备求职项目展示价值。
- 首版纯前端、本地优先，不开发后端和账号系统。
- 使用 React + TypeScript + Vite。
- 视觉采用开发者知识库风格。
- 首版代码题只提供书写和答案对照，不执行代码。
- 使用自评而非 AI 自动评分。
- 使用固定间隔复习，不引入复杂记忆算法。
- 题库构建时转换，避免运行时解析超大 Markdown。
- 首版覆盖现有全部 115 道题。