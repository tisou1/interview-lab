var e=JSON.parse(`[{"id":1,"slug":"question-1","title":"执行上下文、词法环境和调用栈是什么关系？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"<p>请说明执行上下文、词法环境与调用栈三者的关系，并解释标识符查找的顺序。</p>","answerSections":[{"id":"q1-section-0","title":"核心答案","html":"<p>执行上下文是「一次代码执行所需的全部运行信息」的抽象，可以理解为它关联当前代码、词法环境与 <code>this</code>；词法环境负责标识符绑定并沿 outer reference 形成作用域链；调用栈则是承载这些上下文的栈式结构。三者不是同一层概念，不要混着说。</p>\\n"},{"id":"q1-section-1","title":"原理与示例","html":"\\n<ul>\\n<li><strong>词法环境</strong>由环境记录和 outer reference 组成。环境记录分对象式（<code>var</code>、函数声明）与声明式（<code>let</code>、<code>const</code>、<code>class</code>）两类，这也是同一次执行里 <code>var</code> 与 <code>let</code> 行为不同的原因之一。</li>\\n<li><strong>变量查找顺序</strong>：先查当前环境记录，找不到就沿 outer reference 逐层向外，直到全局环境；仍找不到才抛 <code>ReferenceError</code>。</li>\\n<li><strong>作用域链由源码的词法嵌套位置决定</strong>，与函数在哪里被调用无关，这是闭包能读到外层变量的原因。</li>\\n<li><strong>调用栈</strong>只保存当前未返回的调用帧；每个帧持有对应上下文的 <code>this</code>、<code>new.target</code>、返回位置等信息。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">let size = 'global'\\r\\n\\r\\nfunction outer() {\\r\\n  let size = 'outer'\\r\\n  return () => size // 沿词法环境向外找到 outer 的 size\\r\\n}\\r\\n\\r\\nconsole.log(outer()()) // 'outer'\\n</code></pre>\\n"},{"id":"q1-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>「创建阶段/执行阶段」「变量对象（VO）/活动对象（AO）」来自 ES5 之前或教学抽象，用来理解可以，但不要宣称它们是现代规范的内部实现；ES2015+ 之后规范使用 Lexical Environment 与 Environment Record 描述。</li>\\n<li>全局环境在浏览器中还有 <code>globalThis</code> 对象记录（<code>var</code> 与函数声明会成为其属性，<code>let</code>/<code>const</code> 不会），这与浏览器 <code>window</code> 同名属性可以互相覆盖有关。</li>\\n<li>调用栈溢出抛出的是 <code>RangeError</code>，不是 <code>ReferenceError</code>，也不要和堆内存不足（<code>RangeError: Invalid string length</code> 之类）混为一谈。</li>\\n</ul>\\n"},{"id":"q1-section-3","title":"追问","html":"\\n<ul>\\n<li>递归太深为什么栈溢出？因为每次未返回的调用都占一个栈帧，深度超过引擎限制即抛错；改写成循环或显式栈可以把空间搬到堆上。</li>\\n<li><code>this</code> 和词法环境有什么关系？<code>this</code> 由调用方式决定（箭头函数除外，它继承外层），与变量沿作用域链查找是两套独立规则。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":32}},{"id":2,"slug":"question-2","title":"什么是变量提升？var、let、const 和函数声明有什么区别？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q2-section-0","title":"核心答案","html":"<p>\\"提升\\"是\\"进入作用域时先建立绑定\\"这一行为的观察结果，不是引擎真的把源码搬到顶部。<code>var</code> 的绑定会被初始化为 <code>undefined</code>；<code>let</code>/<code>const</code> 的绑定在声明语句执行前处于未初始化状态（TDZ），访问会抛 <code>ReferenceError</code>。</p>\\n"},{"id":"q2-section-1","title":"原理与示例","html":"\\n<ul>\\n<li>环境记录分两类：对象式记录承载 <code>var</code> 与函数声明，声明式记录承载 <code>let</code>、<code>const</code>、<code>class</code>，这也是同一作用域内两者行为不同的根源。</li>\\n<li><strong>函数声明</strong>在进入作用域时即完成绑定初始化，所以声明前可调用；<strong>函数表达式</strong>本质是赋值，声明前是 <code>undefined</code> 或 TDZ。</li>\\n<li><code>const</code> 限制的是绑定不可重新赋值，对象内容仍然可变。</li>\\n<li><code>class</code> 与 <code>let</code> 一样存在 TDZ，<code>new C()</code> 写在类声明之前会抛错。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">console.log(a) // undefined\\r\\nvar a = 1\\r\\n\\r\\nconsole.log(b) // ReferenceError: Cannot access 'b' before initialization\\r\\nlet b = 2\\r\\n\\r\\nsayHi() // OK：函数声明已初始化\\r\\nfunction sayHi() {}\\n</code></pre>\\n"},{"id":"q2-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><code>typeof undeclaredVar</code> 返回 <code>'undefined'</code> 不抛错；但 <code>typeof tdzVar</code> 会抛 <code>ReferenceError</code>，因为绑定已存在、只是尚未初始化。这两个\\"undefined\\"含义不同。</li>\\n<li>同一作用域内 <code>let</code> 与 <code>var</code> 重复声明同名变量是语法阶段的 <code>SyntaxError</code>（整段脚本都不执行），而重复 <code>var</code> 声明只是静默覆盖。</li>\\n<li>块级函数声明（<code>if (x) { function f() {} }</code>）在严格模式和历史 sloppy 模式、不同引擎中行为不一致，现代代码不要依赖这种边界。</li>\\n<li>顶层 <code>var</code> 与函数声明会成为 <code>globalThis</code> 的属性，<code>let</code>/<code>const</code> 不会。</li>\\n</ul>\\n"},{"id":"q2-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>为什么要引入 TDZ？</strong> 把\\"声明前使用\\"从静默拿到 <code>undefined</code> 变成显式报错，配合 <code>const</code> 帮助尽早暴露逻辑错误。</li>\\n<li><strong><code>for (let i...)</code> 为什么每轮都有独立的 <code>i</code>？</strong> 规范为每次迭代创建新的环境记录并复制上一轮的值，因此闭包捕获到的是每一轮各自的绑定。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":76}},{"id":3,"slug":"question-3","title":"闭包到底保存了什么？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript","闭包"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q3-section-0","title":"核心答案","html":"<p>闭包是函数与其创建时可访问的词法环境的组合。它保存的是<strong>绑定本身</strong>，不是值的快照；只要闭包仍可达，它引用的环境记录就不会被回收。</p>\\n"},{"id":"q3-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">function createCounter() {\\r\\n  let count = 0\\r\\n  return () => ++count\\r\\n}\\r\\n\\r\\nconst a = createCounter()\\r\\nconst b = createCounter()\\r\\na() // 1\\r\\na() // 2\\r\\nb() // 1\\n</code></pre>\\n<p><code>a</code> 与 <code>b</code> 分别关联两次调用产生的不同环境记录，因此各有一份 <code>count</code>。同一环境被多个闭包共享时，它们看到的是同一个绑定：</p>\\n<pre><code class=\\"language-js\\">function pair() {\\r\\n  let n = 0\\r\\n  return [() => ++n, () => n]\\r\\n}\\r\\nconst [inc, read] = pair()\\r\\ninc()\\r\\nread() // 1：两个闭包共享同一个 n\\n</code></pre>\\n"},{"id":"q3-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>闭包本身不是内存泄漏。只有当闭包长期可达，且不必要地引用大对象、DOM 节点、监听器或缓存时，相关对象才无法回收。</li>\\n<li>真实泄漏多半来自生命周期管理失误：定时器/监听器未清理、被闭包捕获的 DOM 引用、只增不减的全局缓存。定位靠 Memory 面板做前后 heap snapshot 对比，而不是凭感觉断言。</li>\\n<li>引擎会对闭包做逃逸分析，可能只保留被实际引用的绑定，但这是实现细节，不能作为正确性依赖。</li>\\n<li><code>for (var i = ...)</code> 共享函数级绑定，<code>for (let i = ...)</code> 每次迭代新建绑定，这是异步回调里\\"都打印同一个值\\"的根因。</li>\\n</ul>\\n"},{"id":"q3-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>回调读到的变量为什么不对？</strong> 通常是与外层共享了同一个绑定；用 <code>let</code> 或把当前值作为参数传入即可隔离。</li>\\n<li><strong>闭包与 <code>this</code> 有关吗？</strong> 无关。<code>this</code> 由调用方式在运行时决定，只有箭头函数例外（它不捕获 <code>this</code>，也不拥有自己的 <code>this</code>）。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":119}},{"id":4,"slug":"question-4","title":"this 的值如何确定？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q4-section-0","title":"核心答案","html":"<p>普通函数的 <code>this</code> 由<strong>调用方式</strong>在运行时决定，不由定义位置决定。判定优先级从高到低：<code>new</code> 调用 → 显式 <code>call</code>/<code>apply</code>/<code>bind</code> → 方法调用（<code>.</code> 或 <code>[]</code> 左侧的对象）→ 默认绑定。</p>\\n"},{"id":"q4-section-1","title":"原理与示例","html":"\\n<ul>\\n<li>默认绑定：严格模式下 <code>this</code> 是 <code>undefined</code>，非严格模式是全局对象。</li>\\n<li><code>bind</code> 返回绑定后的新函数，绑定结果不能再被 <code>call</code>/<code>apply</code> 覆盖。</li>\\n<li>箭头函数没有自己的 <code>this</code>（也没有 <code>arguments</code>、<code>new.target</code>），它沿用定义时外层函数的 <code>this</code>，因此不能作为构造函数。</li>\\n<li>类体默认严格模式；类字段中的箭头函数会捕获创建时实例的 <code>this</code>。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">const user = {\\r\\n  name: 'A',\\r\\n  normal() { return this.name },\\r\\n  arrow: () => this?.name,\\r\\n}\\r\\n\\r\\nuser.normal() // 'A'：调用点是 user\\r\\nconst detached = user.normal\\r\\ndetached() // undefined 或报错：丢失了接收者\\r\\nuser.arrow() // 取决于定义位置的外层 this，与 user 无关\\n</code></pre>\\n"},{"id":"q4-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>把方法当回调传递必然丢接收者（<code>setTimeout(user.normal, 0)</code>、<code>arr.map(user.normal)</code>），需要用 <code>bind</code> 或箭头函数包装。</li>\\n<li>事件监听器里的 <code>this</code> 是 <code>currentTarget</code>（普通函数），而 <code>event.target</code> 是实际触发元素；箭头函数拿不到 <code>this</code>，但可以用 <code>event.currentTarget</code>。</li>\\n<li>非严格模式与严格模式的默认绑定不同，而 ESM、类体、<code>\\"use strict\\"</code> 都是严格模式，所以不要再按\\"<code>this</code> 默认是 window\\"背结论。</li>\\n<li><code>new (obj.fn)()</code> 走 <code>new</code> 规则，<code>this</code> 是新建实例而不是 <code>obj</code>。</li>\\n</ul>\\n"},{"id":"q4-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>怎样在回调里保住 <code>this</code>？</strong> 首选箭头函数或显式 <code>bind</code>；避免用 <code>const self = this</code> 这类遗留写法。</li>\\n<li><strong>箭头函数适合当对象方法吗？</strong> 一般不适合，它拿不到调用者对象；适合用在需要固定外层 <code>this</code> 的回调里。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":171}},{"id":5,"slug":"question-5","title":"new Foo() 做了什么？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q5-section-0","title":"核心答案","html":"<p><code>new Foo()</code> 的核心是四步：创建新对象 → 把新对象的 <code>[[Prototype]]</code> 指向 <code>Foo.prototype</code> → 以新对象为 <code>this</code> 执行 <code>Foo</code> → 若构造函数返回对象类型的值就采用它，否则返回新对象。</p>\\n"},{"id":"q5-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">function myNew(Ctor, ...args) {\\r\\n  if (typeof Ctor !== 'function') throw new TypeError('Ctor is not a constructor')\\r\\n  const instance = Object.create(Ctor.prototype ?? Object.prototype)\\r\\n  const result = Ctor.apply(instance, args)\\r\\n  return result !== null &#x26;&#x26; (typeof result === 'object' || typeof result === 'function')\\r\\n    ? result\\r\\n    : instance\\r\\n}\\n</code></pre>\\n<p>这段只是教学近似：真实语义还包含 <code>new.target</code> 的传递、类只能通过 <code>[[Construct]]</code> 调用、内置构造函数依赖 <code>new.target</code> 区分调用方式等。</p>\\n"},{"id":"q5-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>构造函数返回<strong>原始值</strong>（<code>return 1</code>）或 <code>null</code> 时会被忽略，仍然返回新对象；只有对象/函数类型的返回值才生效。</li>\\n<li>箭头函数没有 <code>[[Construct]]</code>，不能用 <code>new</code>；类构造函数不加 <code>new</code> 调用会抛 <code>TypeError</code>。</li>\\n<li><code>Foo.prototype</code> 不是对象时（如被赋值为 <code>1</code>），<code>new</code> 会退化为使用 <code>Object.prototype</code> 作为原型，这是规范行为但属于糟糕写法。</li>\\n<li>近似实现不能替代 <code>Reflect.construct</code>：类构造函数无法用 <code>apply</code> 调用，派生类的 <code>new.target</code> 语义也无法还原。</li>\\n<li>判断构造来源用 <code>new.target</code>，不要用 <code>this instanceof Foo</code>，后者在派生类中会误判。</li>\\n</ul>\\n"},{"id":"q5-section-3","title":"追问","html":"\\n<ul>\\n<li><strong><code>new.target</code> 有什么用？</strong> 区分\\"直接调用\\"与\\"<code>new</code> 调用\\"，实现抽象基类检查，或在派生类中把真正的构造目标继续传给父类。</li>\\n<li><strong><code>instanceof</code> 能替代类型判断吗？</strong> 不能完全替代：它依赖原型链，跨 realm（iframe、worker 反序列化）会失效，也可能被 <code>Symbol.hasInstance</code> 改写。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":216}},{"id":6,"slug":"question-6","title":"原型链如何工作？prototype 与 __proto__ 有什么区别？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript","原型链"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q6-section-0","title":"核心答案","html":"<p>每个对象都有内部槽 <code>[[Prototype]]</code> 指向它的原型对象，属性读取沿这条链逐层向上直到 <code>null</code>。<code>prototype</code> 与 <code>[[Prototype]]</code> 是两个不同层次的东西：前者是<strong>函数对象上的普通属性</strong>，在 <code>new</code> 时被用作新实例的原型；后者是<strong>对象自身的原型链接</strong>，<code>__proto__</code> 只是访问它的历史遗留访问器。</p>\\n"},{"id":"q6-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">function User() {}\\r\\nconst u = new User()\\r\\n\\r\\nObject.getPrototypeOf(u) === User.prototype // true\\r\\nObject.getPrototypeOf(User.prototype) === Object.prototype // true\\r\\nUser.prototype.__proto__ === Object.prototype // true，与上一行是同一件事\\r\\nObject.getPrototypeOf(Object.prototype) // null，链条终点\\n</code></pre>\\n<ul>\\n<li><strong>读取</strong> <code>obj.x</code>：先查自身属性，再沿 <code>[[Prototype]]</code> 向上；沿链上访问到的 getter 会被执行。</li>\\n<li><strong>写入</strong> <code>obj.x = v</code>：通常创建/修改<strong>自身</strong>属性；如果链上存在同名 setter，会调用该 setter 而不创建自身属性；如果自身是不可写数据属性，严格模式抛 <code>TypeError</code>。</li>\\n</ul>\\n"},{"id":"q6-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>不要用 <code>__proto__</code> 做业务代码的读写：它只是 <code>Object.prototype</code> 上的访问器，<code>Object.create(null)</code> 出来的对象根本没有它。应使用 <code>Object.getPrototypeOf</code> / <code>Object.setPrototypeOf</code> / <code>Object.create</code>。</li>\\n<li><code>Object.setPrototypeOf</code> 会破坏引擎对对象形状的优化，能通过 <code>Object.create</code> 确定原型的就不要事后改原型。</li>\\n<li><code>instanceof</code> 检查\\"构造函数的<strong>当前</strong> <code>prototype</code> 是否在对象原型链上\\"，因此替换 <code>Fn.prototype</code> 后旧实例会失效，跨 realm 也会失效。</li>\\n<li>遍历语义要区分：<code>for...in</code> 会枚举原型链上的可枚举属性，<code>Object.keys</code> 只看自身可枚举字符串键，<code>Reflect.ownKeys</code> 返回自身全部键（含 symbol）。</li>\\n</ul>\\n"},{"id":"q6-section-3","title":"追问","html":"\\n<ul>\\n<li><strong><code>Object.create(null)</code> 有什么实际用途？</strong> 创建无原型对象当纯字典，避免 <code>__proto__</code>、<code>constructor</code> 这类键造成原型污染，也避免误继承内置方法。</li>\\n<li><strong>为什么推荐 <code>Object.hasOwn(obj, key)</code>？</strong> 对象自身可能覆盖 <code>hasOwnProperty</code>，无原型对象上则不存在该方法；<code>Object.hasOwn</code> 是静态方法，不受这两点影响。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":257}},{"id":7,"slug":"question-7","title":"JavaScript class 是不是传统面向对象语言里的类？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q7-section-0","title":"核心答案","html":"<p>不是另一种运行机制：<code>class</code> 建立在原型继承之上，属于语法糖。但它带有一批独立的语言语义，因此也不能简单说\\"就是函数加 <code>prototype</code>\\"。</p>\\n"},{"id":"q7-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">class User {\\r\\n  method() {}      // 定义在 User.prototype 上，不可枚举，各实例共享\\r\\n  field = () => {} // 每个实例自身的属性，每次构造都新建一个函数\\r\\n}\\n</code></pre>\\n<p>与手写构造函数 + <code>prototype</code> 的差异：</p>\\n<ul>\\n<li>类声明存在 TDZ，不像函数声明那样声明前可用。</li>\\n<li>类体整体运行在严格模式。</li>\\n<li>原型方法默认 <code>enumerable: false</code>，手写挂载默认是可枚举的。</li>\\n<li>必须用 <code>new</code> 调用，内部走 <code>[[Construct]]</code>。</li>\\n<li>支持 <code>super</code>、私有字段 <code>#x</code>、静态块、<code>get</code>/<code>set</code>、以及 <code>extends</code> 内置类型（如 <code>Array</code>、<code>Error</code>）。</li>\\n</ul>\\n"},{"id":"q7-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>字段初始化时机：必须先 <code>super()</code> 才能访问 <code>this</code>，而父类构造函数体内如果调用被子类字段\\"覆盖\\"的方法，会读到字段尚未初始化的状态。</li>\\n<li>类字段用箭头函数会为每个实例复制一个函数，内存上不如原型方法，但能自动绑定 <code>this</code>；按使用场景取舍，不要无脑统一。</li>\\n<li>私有字段 <code>#x</code> 是语法层面的真私有（外部无法访问、不能通过 <code>Reflect.get</code> 读取），与 <code>_x</code> 这种命名约定完全不同。</li>\\n<li><code>typeof MyClass</code> 在类声明之前会抛错（TDZ），不能靠 <code>typeof</code> 做\\"未定义\\"探测。</li>\\n</ul>\\n"},{"id":"q7-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>子类不写 constructor 会怎样？</strong> 等价于 <code>constructor(...args) { super(...args) }</code>，由默认派生构造完成。</li>\\n<li><strong><code>super</code> 的两种用法？</strong> <code>super(...)</code> 调用父类构造函数；<code>super.method()</code> 在父类原型上查找方法，但 <code>this</code> 仍是当前实例。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":297}},{"id":8,"slug":"question-8","title":"属性描述符有什么用？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q8-section-0","title":"核心答案","html":"<p>属性描述符精确控制一个属性的读、写、枚举与重定义能力。数据属性用 <code>value/writable/enumerable/configurable</code>，访问器属性用 <code>get/set/enumerable/configurable</code>，两类互斥，不能混写。</p>\\n"},{"id":"q8-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">const obj = {}\\r\\nObject.defineProperty(obj, 'id', {\\r\\n  value: 1,\\r\\n  writable: false,\\r\\n  enumerable: false,\\r\\n  configurable: false,\\r\\n})\\r\\n\\r\\nobj.id = 2       // 非严格模式静默失败，严格模式抛 TypeError\\r\\nObject.keys(obj) // []：不可枚举，不参与遍历与展开\\r\\ndelete obj.id    // 非严格模式静默失败，严格模式抛 TypeError\\n</code></pre>\\n<ul>\\n<li><code>Object.defineProperty</code> 未显式给出的标志<strong>默认为 <code>false</code></strong>；而对象字面量创建属性时四个标志默认都是 <code>true</code>。这是最容易踩的差异。</li>\\n<li><code>Object.freeze</code> 置为不可写且不可配置，<code>Object.seal</code> 只禁止增删、允许改值；两者都是<strong>浅层</strong>。</li>\\n<li>键的枚举层次：<code>Object.keys</code>（自身可枚举字符串键）⊂ <code>Object.getOwnPropertyNames</code>（自身字符串键）⊂ <code>Reflect.ownKeys</code>（再加 symbol 键）。</li>\\n</ul>\\n"},{"id":"q8-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><code>writable: false</code> 不等于值不可变：如果值是对象，其内部属性仍可修改。真正的不可变需要递归冻结或使用不可变数据结构。</li>\\n<li><code>configurable: false</code> 无法恢复为 <code>true</code>；此时不能把数据属性改成访问器属性（反之亦然），只能修改 <code>value</code>（且 <code>writable</code> 为 <code>true</code> 时）。</li>\\n<li>展开与 <code>Object.assign</code> 只复制自身可枚举属性，会触发 getter、丢弃描述符与原型，且都是浅拷贝。</li>\\n<li><code>Object.freeze</code> 不会冻结 <code>Map</code>/<code>Set</code> 的内部内容，也不能阻止通过其他引用修改嵌套对象。</li>\\n</ul>\\n"},{"id":"q8-section-3","title":"追问","html":"\\n<ul>\\n<li><strong><code>enumerable: false</code> 有什么用？</strong> 让属性不参与遍历/展开（内置原型方法就是如此），也可用于隐藏内部状态。</li>\\n<li><strong>响应式系统为什么关心描述符？</strong> Vue 2 用 <code>Object.defineProperty</code> 递归改写 getter/setter 来收集依赖，因此无法侦测新增属性和数组下标赋值，需要 <code>Vue.set</code> 兜底；Vue 3 改用 <code>Proxy</code> 拦截整个对象来解决。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":339}},{"id":9,"slug":"question-9","title":"JavaScript 的值类型与引用语义应该怎样准确表述？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q9-section-0","title":"核心答案","html":"<p>JavaScript 的参数传递<strong>始终是按值传递</strong>。差异来自值本身是什么：原始值复制的是值本身；对象复制的是\\"指向该对象的引用\\"这个值，因此两个变量指向同一个对象。</p>\\n"},{"id":"q9-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">function mutate(x) { x.name = 'B' }\\r\\nfunction replace(x) { x = { name: 'C' } }\\r\\n\\r\\nconst user = { name: 'A' }\\r\\nmutate(user)  // user.name === 'B'：通过引用改到同一个对象\\r\\nreplace(user) // user 仍是 { name: 'B' }：给形参重新赋值不影响调用方\\n</code></pre>\\n<ul>\\n<li>变量是绑定，保存的要么是原始值，要么是对象引用。</li>\\n<li><code>const</code> 约束的是绑定不能重新赋值，不约束对象内部状态的变化。</li>\\n</ul>\\n"},{"id":"q9-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>不要说\\"对象是引用传递\\"。引用传递（如 C++ 的 <code>&#x26;</code>）意味着给形参赋值会改变调用方变量，JS 做不到——这正是 <code>replace(user)</code> 无效的原因。</li>\\n<li>\\"原始值 vs 引用值\\"是常见简化说法；更准确的表述是\\"值本身是否为对象\\"，<code>typeof</code>/装箱等细节会带来偏差。</li>\\n<li>比较对象用 <code>===</code> 比较的是引用身份，不是内容；<code>Object.is</code> 的差异只在 <code>NaN</code> 与 <code>-0</code>。</li>\\n<li>字符串等原始值不可变，任何\\"修改\\"都是产生新值再重新绑定。</li>\\n</ul>\\n"},{"id":"q9-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>为什么 <code>const</code> 声明的数组还能 <code>push</code>？</strong> 绑定没变，变的是对象内部状态。</li>\\n<li><strong>怎样避免共享引用带来的副作用？</strong> 在边界处复制（浅/深拷贝，见第 12 题），或按不可变更新约定只替换变化路径。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":384}},{"id":10,"slug":"question-10","title":"==、=== 与 Object.is 有什么差异？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q10-section-0","title":"核心答案","html":"<p>三者的差异集中在两点：是否做类型转换、如何处理 <code>NaN</code> 与 <code>-0</code>。<code>===</code> 不做类型转换，但 <code>NaN !== NaN</code> 且 <code>0 === -0</code>；<code>Object.is</code> 使用 SameValue 语义，<code>NaN</code> 等于自身、<code>0</code> 与 <code>-0</code> 不相等；<code>==</code> 会走抽象相等比较并按规则转换类型。</p>\\n"},{"id":"q10-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">0 === -0            // true\\r\\nObject.is(0, -0)    // false\\r\\nNaN === NaN         // false\\r\\nObject.is(NaN, NaN) // true\\r\\n\\r\\nnull == undefined   // true\\r\\n'' == 0             // true\\r\\n'1' == 1            // true\\r\\n[] == false         // true\\n</code></pre>\\n<p><code>==</code> 的判定顺序：同类型直接比较；<code>null</code> 与 <code>undefined</code> 互相相等且不等于其他值；数字与字符串比较时转数字；布尔先转数字；对象与原始值比较时先用 <code>ToPrimitive</code> 转换对象。</p>\\n"},{"id":"q10-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><code>Object.is</code> 与 <code>===</code> <strong>只在 <code>NaN</code> 和 <code>-0</code> 上有差异</strong>，不要以为它是全面更严格的比较。它的语义是把 <code>NaN</code> 视为相等，因此常被用于\\"值是否相同\\"的判断。</li>\\n<li>React 在 <code>useState</code> 的更新 bailout、<code>useEffect</code>/<code>useMemo</code> 的依赖比较中采用 <code>Object.is</code> 语义。这解释了：写入 <code>NaN</code> 不会被视为变化，而 <code>0</code> 与 <code>-0</code> 会被视为不同值。</li>\\n<li><code>x == null</code> 是少数被团队普遍接受的简写（同时匹配 <code>null</code> 和 <code>undefined</code>），其他场景优先 <code>===</code>，并用 lint 的 <code>eqeqeq</code> 规则约束。</li>\\n<li><code>==</code> 与 <code>&#x3C;</code>、<code>></code> 的转换规则并不完全一致，不要用同一套直觉推断。</li>\\n</ul>\\n"},{"id":"q10-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>为什么 <code>[] == false</code> 为 true？</strong> 布尔先转数字得到 <code>0</code>，<code>[]</code> 经 <code>ToPrimitive</code> 得到 <code>\\"\\"</code> 再转成 <code>0</code>，于是相等。</li>\\n<li><strong>什么时候必须用 <code>Object.is</code>？</strong> 需要把 <code>NaN</code>/<code>-0</code> 纳入值语义比较时（如自定义缓存键、实现快照比较），或与框架的相等判断保持一致时。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":423}},{"id":11,"slug":"question-11","title":"隐式类型转换最容易错在哪里？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q11-section-0","title":"核心答案","html":"<p>对象转原始值走 <code>ToPrimitive</code>：优先调用 <code>Symbol.toPrimitive</code>，否则按 hint（<code>number</code>、<code>string</code>、<code>default</code>）依次尝试 <code>valueOf</code> 和 <code>toString</code>。<code>+</code> 同时承担数字加法与字符串拼接，<code>-</code>、<code>*</code>、<code>></code> 等则强制转数字，因此出错几乎都源自\\"没意识到发生了转换\\"。</p>\\n"},{"id":"q11-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">1 + '2'        // '12'：有字符串就是拼接\\r\\n'5' - 2        // 3：算术运算强制转数字\\r\\n[] + []        // ''\\r\\n[] + {}        // '[object Object]'\\r\\nBoolean('0')   // true：非空字符串是真\\r\\nNumber('')     // 0\\r\\nNumber(' 12 ') // 12\\r\\nparseInt('12px') // 12：忽略尾部垃圾\\r\\nNumber('12px')   // NaN\\n</code></pre>\\n"},{"id":"q11-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>关系运算遇到字符串会按字典序比较：<code>'10' &#x3C; '9'</code> 为 <code>true</code>，这是\\"数值比较\\"最常见的踩坑点。</li>\\n<li><code>Number('')</code>、<code>Number(' ')</code>、<code>Number(null)</code> 都是 <code>0</code>，但 <code>Number(undefined)</code> 是 <code>NaN</code>；<code>parseInt</code> 需要显式传 radix，虽然 ES5 后 <code>'08'</code> 不再按八进制解析。</li>\\n<li>浮点精度：<code>0.1 + 0.2 !== 0.3</code>；金额应按最小单位用整数或 <code>BigInt</code> 处理（见第 97 题）。</li>\\n<li><code>BigInt</code> 不能与 <code>Number</code> 隐式混算（<code>1n + 1</code> 抛 <code>TypeError</code>），<code>JSON.stringify</code> 遇到 <code>BigInt</code> 也会抛错。</li>\\n<li>面试重点不是背怪题结论，而是能说清转换路径；工程上应在输入边界显式解析并校验（<code>Number.isNaN</code>、<code>Number.isInteger</code>）。</li>\\n</ul>\\n"},{"id":"q11-section-3","title":"追问","html":"\\n<ul>\\n<li><strong><code>[] == ![]</code> 为什么为 true？</strong> <code>![]</code> 先得到 <code>false</code>，再走 <code>==</code>：一侧转数字 0，另一侧 <code>[]</code> 先 <code>ToPrimitive</code> 成 <code>\\"\\"</code> 再转 0。</li>\\n<li><strong>怎样系统性减少这类问题？</strong> 统一用 <code>===</code>、开启 TypeScript <code>strict</code>、输入边界做显式解析与校验、用 lint 规则约束。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":464}},{"id":12,"slug":"question-12","title":"浅拷贝、深拷贝与结构化克隆有什么区别？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q12-section-0","title":"核心答案","html":"<p>浅拷贝只复制第一层，嵌套对象仍与源共享引用；深拷贝递归复制整棵对象图；<code>structuredClone</code> 是平台提供的结构化克隆算法，支持循环引用和多数内置类型，但不支持函数、DOM 节点与原型语义。</p>\\n"},{"id":"q12-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">const shallow = { ...source }           // 一层；getter 会被求值，描述符与原型不保留\\r\\nconst cloned = structuredClone(source)  // 支持循环引用、Map/Set/Date/ArrayBuffer\\r\\n\\r\\nfunction deepClone(value, seen = new WeakMap()) {\\r\\n  if (value === null || typeof value !== 'object') return value\\r\\n  if (seen.has(value)) return seen.get(value) // 关键：显式处理循环引用\\r\\n  const copy = Array.isArray(value) ? [] : {}\\r\\n  seen.set(value, copy)\\r\\n  for (const key of Reflect.ownKeys(value)) copy[key] = deepClone(value[key], seen)\\r\\n  return copy\\r\\n}\\n</code></pre>\\n"},{"id":"q12-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><code>JSON.parse(JSON.stringify(x))</code> <strong>不是</strong>可靠深拷贝：丢失 <code>undefined</code>、函数、symbol，<code>Date</code> 变字符串，<code>Map</code>/<code>Set</code> 变 <code>{}</code>，<code>BigInt</code> 抛错，循环引用抛错，<code>NaN</code>/<code>Infinity</code> 变 <code>null</code>。</li>\\n<li><code>structuredClone</code> 不能克隆函数、DOM 节点、<code>Proxy</code>；类实例会丢失原型变成普通对象，<code>getter</code>/<code>setter</code> 会被求值成普通属性。<code>ArrayBuffer</code> 可通过 <code>transfer</code> 转移所有权而非复制。</li>\\n<li>浅拷贝手段（展开、<code>Object.assign</code>、<code>slice</code>、<code>concat</code>）都只处理第一层，且不复制描述符与原型。</li>\\n<li>React 场景中整体深拷贝通常是坏味道：既昂贵又会切断引用比较带来的性能收益，正确做法是只复制变化路径。</li>\\n</ul>\\n"},{"id":"q12-section-3","title":"追问","html":"\\n<ul>\\n<li><strong><code>structuredClone</code> 为什么能处理循环引用？</strong> 内部维护已访问对象的映射（与上面 <code>WeakMap</code> 的思路一致）。</li>\\n<li><strong>什么时候真的需要深拷贝？</strong> 跨边界传递会被外部修改的数据、需要独立快照、或必须切断引用共享时；其余情况优先不可变更新。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":504}},{"id":13,"slug":"question-13","title":"浏览器 Event Loop 如何运行？","category":"javascript","type":"theory","difficulty":"basic","tags":["JavaScript","Event Loop"],"estimatedMinutes":3,"promptHtml":"","answerSections":[{"id":"q13-section-0","title":"核心答案","html":"<p>事件循环按轮推进：每轮取出一个宏任务执行完 → 清空微任务队列 → 浏览器在合适时机更新渲染 → 进入下一轮。宏任务包括定时器回调、用户事件、网络回调、<code>MessageChannel</code> 等；微任务包括 Promise reaction、<code>queueMicrotask</code>、<code>MutationObserver</code>。</p>\\n"},{"id":"q13-section-1","title":"原理与示例","html":"\\n<ul>\\n<li><strong>微任务在当前宏任务结束后、渲染之前全部清空</strong>，这是 Promise 回调总比 <code>setTimeout</code> 先执行的原因。</li>\\n<li><code>setTimeout(fn, 0)</code> 只保证\\"不早于阈值\\"：主线程被占用、后台标签页节流、嵌套超过 5 层后浏览器最小 4ms 延迟都会让它更晚。</li>\\n<li><code>requestAnimationFrame</code> 回调在下一帧渲染前执行，属于渲染步骤，不是普通宏任务；<code>requestIdleCallback</code> 只在空闲时段运行。</li>\\n<li>一次宏任务只取一个：长同步代码会阻塞渲染与输入响应。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">console.log('sync')\\r\\nsetTimeout(() => console.log('macro'), 0)\\r\\nPromise.resolve().then(() => console.log('micro'))\\r\\n// sync → micro → macro\\n</code></pre>\\n"},{"id":"q13-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>微任务里持续追加微任务会<strong>饿死</strong>渲染与后续宏任务（<code>while (true) await tick()</code> 会卡死页面）。</li>\\n<li>规范只规定\\"浏览器在合适时机渲染\\"，不保证每轮都渲染，也不保证每帧与宏任务一一对应。</li>\\n<li>Node.js 的事件循环有独立阶段划分（timers、poll、check 等），不要用浏览器的顺序表解释 Node 行为。</li>\\n<li>微任务结束后立刻读取布局会强制同步布局，把读写混在一起会造成 layout thrashing。</li>\\n</ul>\\n"},{"id":"q13-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>为什么 <code>setTimeout(fn, 0)</code> 有时明显延迟？</strong> 主线程被长任务占用、页面处于后台被节流，或嵌套层级触发最小延迟。</li>\\n<li><strong>微任务适合做什么？</strong> 需要在\\"本轮同步代码全部结束、渲染之前\\"完成的一致性收尾，例如框架的批量更新，Vue 的 <code>nextTick</code> 就是这一思路。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":545}},{"id":14,"slug":"question-14","title":"输出顺序题：同步代码、Promise 和定时器","category":"javascript","type":"theory","difficulty":"advanced","tags":["JavaScript","Promise"],"estimatedMinutes":8,"promptHtml":"<pre><code class=\\"language-js\\">console.log('A')\\r\\n\\r\\nsetTimeout(() => console.log('B'), 0)\\r\\n\\r\\nPromise.resolve()\\r\\n  .then(() => {\\r\\n    console.log('C')\\r\\n    queueMicrotask(() => console.log('D'))\\r\\n  })\\r\\n  .then(() => console.log('E'))\\r\\n\\r\\nqueueMicrotask(() => console.log('F'))\\r\\nconsole.log('G')\\n</code></pre>","answerSections":[{"id":"q14-section-0","title":"核心答案","html":"<p>输出 <code>A G C F D E B</code>。</p>\\n"},{"id":"q14-section-1","title":"原理与示例","html":"\\n<p>按阶段推导，而不是靠直觉：</p>\\n<ol>\\n<li><strong>同步阶段</strong>：打印 <code>A</code>；注册宏任务（B）；<code>Promise.resolve().then(C)</code> 把 C 的反应入队；<code>queueMicrotask(F)</code> 把 F 入队；打印 <code>G</code>。</li>\\n<li>此刻微任务队列为 <code>[C, F]</code>。</li>\\n<li>执行 C：打印 <code>C</code>，并在回调里把 D 入队；C 的回调返回后，前一个 promise 落定，<code>.then(E)</code> 的反应才入队。队列变为 <code>[F, D, E]</code>。</li>\\n<li>依次执行 F、D、E，打印 <code>F D E</code>。微任务队列清空。</li>\\n<li>进入下一个宏任务，执行定时器回调，打印 <code>B</code>。</li>\\n</ol>\\n<p>关键顺序点：E 要等 C 所在的 reaction 完成后由派生 promise 落定才入队，因此排在 D 之后，而不是紧随 C。</p>\\n"},{"id":"q14-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><code>then</code> 的回调总是异步执行，不存在\\"同步 then\\"。</li>\\n<li>这道题背顺序没有意义：把 <code>queueMicrotask</code> 换成 <code>setTimeout(…, 0)</code>、把链改成 <code>await</code>、或加上 <code>Promise.all</code>，结果都会变，必须能重新推导。</li>\\n<li><code>setTimeout</code> 的最小延迟与后台节流会让 B 更晚，但不改变其他元素的相对顺序。</li>\\n</ul>\\n"},{"id":"q14-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>为什么 D 一定在 E 之前？</strong> D 是在 C 回调里主动入队的；E 的入队时机是 C 所在 reaction 结束后。</li>\\n<li><strong>如果第一个 then 里抛错会怎样？</strong> E 被跳过，错误沿链穿透到下一个 catch 或全局 <code>unhandledrejection</code>。</li>\\n</ul>\\n"},{"id":"q14-section-4","title":"评分点","html":"\\n<p>不仅说出顺序，还能画出每一步的队列变化，并能口述\\"哪个操作导致哪个 reaction 入队\\"。</p>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":584}},{"id":15,"slug":"question-15","title":"async/await 的本质是什么？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q15-section-0","title":"核心答案","html":"<p><code>async</code> 函数的返回值总是 Promise（普通值被包装为 fulfilled，抛错变成 rejected）；<code>await</code> 把其后的代码变成\\"等 Promise settle 后由微任务恢复\\"的 continuation，因此不会阻塞线程。</p>\\n"},{"id":"q15-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">async function run() {\\r\\n  console.log(1)\\r\\n  await 0\\r\\n  console.log(2)\\r\\n}\\r\\n\\r\\nconsole.log(3)\\r\\nrun()\\r\\nconsole.log(4)\\r\\n// 3 1 4 2\\n</code></pre>\\n<ul>\\n<li>即使 <code>await</code> 的是非 Promise 值，后续代码也<strong>不会</strong>在当前同步执行栈里继续，而是被安排到微任务队列。</li>\\n<li><code>await p</code> 近似等于先对 <code>p</code> 做 <code>Promise.resolve(p)</code> 再 <code>then</code>，因此 thenable 会被展开，非 thenable 也会产生一次异步跳转。</li>\\n<li><code>async</code> 函数里 <code>throw</code> 等价于返回 rejected Promise，<code>return value</code> 等价于 resolve。</li>\\n</ul>\\n"},{"id":"q15-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><code>try/catch</code> 能捕获 <code>await</code> 到的 rejection，但捕获不到\\"忘记 await 的 Promise\\"和回调内部抛出的错误——这类错误会变成 <code>unhandledrejection</code>。</li>\\n<li>循环里逐次 <code>await</code> 是<strong>串行</strong>等待、耗时叠加；要并发应先 <code>Promise.all(tasks.map(...))</code> 再统一等待。</li>\\n<li><code>return await promise</code> 与 <code>return promise</code> 语义不同：前者多一次微任务跳转，且能被本函数的 <code>try/catch</code>/<code>finally</code> 捕获；后者直接返回，外层才能接住错误。性能差异可忽略，关键是清楚错误归属。</li>\\n<li>async 函数被调用时函数体<strong>立即同步执行</strong>到第一个 <code>await</code>，不是等到被 await 才启动。</li>\\n</ul>\\n"},{"id":"q15-section-3","title":"追问","html":"\\n<ul>\\n<li><strong><code>await</code> 会阻塞主线程吗？</strong> 不会，它只挂起当前函数并把控制权交回事件循环；但 <code>await</code> 右侧的表达式本身是同步求值的。</li>\\n<li><strong>多个 <code>await</code> 为什么变慢？</strong> 串行等待把延迟相加；只要能并发，就应先发起再统一等待。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":640}},{"id":16,"slug":"question-16","title":"Promise 链的值与错误如何传播？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript","Promise"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q16-section-0","title":"核心答案","html":"<p><code>then</code>、<code>catch</code>、<code>finally</code> 每次都返回<strong>新的 Promise</strong>，新 Promise 的状态由回调的返回值或抛错决定；没有对应处理器的状态会沿链向后穿透。</p>\\n"},{"id":"q16-section-1","title":"原理与示例","html":"\\n<ul>\\n<li>回调返回普通值 → 新 Promise fulfilled 为该值。</li>\\n<li>回调返回 Promise 或 thenable → 新 Promise 采用它的最终状态（会等待）。</li>\\n<li>回调抛错 → 新 Promise rejected。</li>\\n<li>缺少对应 handler → 状态与原因直接穿透到下一个匹配的处理器。</li>\\n<li><code>finally</code> 不改变原值与原拒绝原因，但它在抛错或返回 rejected Promise 时<strong>会覆盖</strong>原结果。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">Promise.resolve(1)\\r\\n  .then(x => x + 1)\\r\\n  .then(() => { throw new Error('x') })\\r\\n  .catch(() => 10)\\r\\n  .finally(() => 20) // 返回的 20 被忽略，finally 不改变结果\\r\\n  .then(console.log) // 10\\n</code></pre>\\n"},{"id":"q16-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><code>.catch()</code> 之后链条恢复为 fulfilled，错误不再往下传播；只想记日志时必须重新 <code>throw</code>，否则调用方以为成功。</li>\\n<li>在 <code>then</code> 里忘记 <code>return</code> 会产生\\"火忘\\"式并发，错误逃出当前链，变成 <code>unhandledrejection</code>。</li>\\n<li><code>finally</code> 里做异步清理必须 <code>return</code>，否则链不会等待清理完成。</li>\\n<li><code>.catch</code> 也属于 handler，它里面再抛错会让新 Promise 变成 rejected；<code>catch</code> 不是\\"错误终点\\"。</li>\\n</ul>\\n"},{"id":"q16-section-3","title":"追问","html":"\\n<ul>\\n<li><strong><code>Promise.reject(e).then(fn)</code> 会执行 fn 吗？</strong> 不会，缺少 onRejected 时直接穿透到下一个 catch 或全局兜底。</li>\\n<li><strong>怎么保证错误一定能被观测？</strong> 每条异步链路都要有归属：能处理的就地处理，不能处理的补上下文后抛出，并在应用边界统一上报。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":684}},{"id":17,"slug":"question-17","title":"Promise.all/allSettled/race/any 如何选择？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript","Promise"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q17-section-0","title":"核心答案","html":"<p>按\\"需要什么结果语义\\"来选：结果缺一不可用 <code>all</code>，需要逐项成败全量汇总用 <code>allSettled</code>，只关心最快结束用 <code>race</code>，只关心第一个成功用 <code>any</code>。四者都<strong>不会取消</strong>其余任务。</p>\\n"},{"id":"q17-section-1","title":"原理与示例","html":"\\n<ul>\\n<li><code>all</code>：全部 fulfilled 才 fulfilled；任一 rejected 立即 rejected（其余任务仍在跑，结果被丢弃）。</li>\\n<li><code>allSettled</code>：等全部 settled，返回 <code>{ status, value | reason }</code> 数组，永不 reject。</li>\\n<li><code>race</code>：第一个 settled 决定结果，成功或失败都可能赢，常用于超时竞争。</li>\\n<li><code>any</code>：第一个 fulfilled 即成功；全部 rejected 时以 <code>AggregateError</code> 拒绝。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">const results = await Promise.allSettled(urls.map(fetchJson))\\r\\nconst ok = results.filter(r => r.status === 'fulfilled').map(r => r.value)\\n</code></pre>\\n"},{"id":"q17-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>空数组的边界：<code>all</code>/<code>allSettled</code> 立即 fulfilled 为 <code>[]</code>，<code>race</code> 永远 pending，<code>any</code> 立即以 <code>AggregateError</code> 拒绝。</li>\\n<li>忽略结果不等于取消：真正释放连接需要 <code>AbortController</code>（或 <code>AbortSignal.timeout</code>），否则请求继续占用带宽与后端资源。</li>\\n<li><code>all</code> 快速失败后，其它请求的结果无人处理，容易出现\\"接口成功但状态没更新\\"的困惑。</li>\\n<li>手写实现必须<strong>按输入顺序</strong>写回结果（用索引赋值），不能 <code>push</code>，否则顺序会随完成时间变化。</li>\\n</ul>\\n"},{"id":"q17-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>超时该用 <code>race</code> 还是 <code>AbortSignal.timeout</code>？</strong> 优先 abort：<code>race</code> 只是忽略结果，不释放底层资源。</li>\\n<li><strong><code>AggregateError</code> 怎么取失败原因？</strong> 遍历 <code>error.errors</code>；用 <code>all</code> 则拿不到全部失败，需要改用 <code>allSettled</code> 自行聚合。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":726}},{"id":18,"slug":"question-18","title":"并发与并行有什么区别？JavaScript 是单线程吗？","category":"javascript","type":"theory","difficulty":"advanced","tags":["JavaScript","并发"],"estimatedMinutes":8,"promptHtml":"","answerSections":[{"id":"q18-section-0","title":"核心答案","html":"<p>并发指多个任务在时间上交错推进（可以只有一个执行单元）；并行指多个任务在同一时刻、不同执行单元上真正同时运行。JavaScript 的<strong>执行模型是单线程</strong>：同一时刻只有一段 JS 在某个调用栈上执行；但宿主环境（浏览器、Node）本身是多线程多进程的。</p>\\n"},{"id":"q18-section-1","title":"原理与示例","html":"\\n<ul>\\n<li>网络请求、HTML 解析、样式计算、栅格化、GC 等由浏览器其他线程/进程完成，通过事件循环把结果交回主线程执行回调。</li>\\n<li><code>Web Worker</code>/<code>Service Worker</code> 提供独立的 JS 执行线程，通过 <code>postMessage</code> 与结构化克隆通信，默认不共享内存；<code>SharedArrayBuffer</code> + <code>Atomics</code> 才共享内存。</li>\\n<li>Node 侧对应 <code>worker_threads</code> 与 <code>cluster</code>。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">// 看起来\\"异步\\"，但仍然是同步的 CPU 占用，会阻塞主线程\\r\\nawait new Promise(resolve => setTimeout(resolve, 0))\\r\\nheavyLoop()\\n</code></pre>\\n"},{"id":"q18-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>把大循环包进 <code>Promise</code> 或 <code>setTimeout</code> <strong>不会</strong>让计算并行，只是把它推到下一轮宏任务，依旧阻塞。</li>\\n<li>Promise 的\\"并发\\"指同时发起、等待结果，与多线程并行无关。</li>\\n<li>时间切片（周期性让出事件循环）改善的是响应性，总耗时不变；要真正减少耗时必须优化算法、用 Worker 或挪到服务端。</li>\\n<li>渲染与 JS 共享主线程，长任务会直接恶化 INP 与动画流畅度。</li>\\n</ul>\\n"},{"id":"q18-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>什么时候值得用 Worker？</strong> CPU 密集、数据可序列化、不需要频繁访问 DOM 时；同时要把拷贝与通信成本算进收益。</li>\\n<li><strong><code>SharedArrayBuffer</code> 为什么需要跨源隔离？</strong> 它允许真正共享内存，需要 COOP/COEP 响应头来降低 Spectre 类侧信道风险。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":763}},{"id":19,"slug":"question-19","title":"Iterator 与 Generator 解决什么问题？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q19-section-0","title":"核心答案","html":"<p>Iterator 是\\"逐步取值\\"的统一协议；Generator 提供了创建可暂停函数并自动实现该协议的语法，用来表达惰性序列、状态机与自定义遍历行为。</p>\\n"},{"id":"q19-section-1","title":"原理与示例","html":"\\n<ul>\\n<li><strong>Iterator 协议</strong>：对象有 <code>next()</code>，返回 <code>{ value, done }</code>。</li>\\n<li><strong>Iterable 协议</strong>：对象有 <code>[Symbol.iterator]()</code> 方法返回 iterator；<code>for...of</code>、展开、解构、<code>Promise.all</code>、<code>yield*</code> 都在消费 iterable。</li>\\n<li>Generator 函数执行到 <code>yield</code> 暂停，<code>next(value)</code> 可把值送回函数，<code>return()</code>/<code>throw()</code> 可提前结束或注入错误。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">function* range(start, end) {\\r\\n  for (let i = start; i &#x3C; end; i++) yield i\\r\\n}\\r\\n\\r\\n[...range(1, 4)] // [1, 2, 3]\\n</code></pre>\\n<p>配合 <code>yield*</code> 委托给另一个 iterable，配合异步生成器 + <code>for await...of</code> 处理流式数据。</p>\\n"},{"id":"q19-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>数组的 <code>map</code>/<code>filter</code> 不消费迭代器协议；自定义 iterable 需要先转换，或自己实现惰性版本。</li>\\n<li>迭代器是<strong>一次性</strong>的：取完后 <code>done</code> 恒为 true，再次 <code>for...of</code> 必须重新获取 iterator。</li>\\n<li>惰性求值会让副作用执行时机变得不直观（generator 函数体内的副作用只在取值时发生）；调用生成器函数本身不会执行函数体，但参数会立即求值。</li>\\n<li>生成器不是异步的：它只提供暂停/恢复，异步能力要靠 async generator 或自行组合 Promise。</li>\\n</ul>\\n"},{"id":"q19-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>普通对象为什么不能用 <code>for...of</code>？</strong> 它默认不是 iterable，需要 <code>Object.entries()</code>/<code>Object.keys()</code> 或自己实现 <code>[Symbol.iterator]</code>。</li>\\n<li><strong>异步生成器有什么用？</strong> 分页流式取数、逐块处理流式响应，比一次性 <code>Promise.all</code> 更适合数据量大的场景。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":800}},{"id":20,"slug":"question-20","title":"Map、Set、WeakMap、WeakSet 如何选择？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q20-section-0","title":"核心答案","html":"<p><code>Map</code>/<code>Set</code> 是强引用的键值集合与唯一值集合，可枚举、可遍历、有 <code>size</code>；<code>WeakMap</code>/<code>WeakSet</code> 弱持有<strong>对象或非注册 symbol</strong> 作为键，不可枚举，适合给对象挂元数据或做与对象生命周期绑定的缓存。</p>\\n"},{"id":"q20-section-1","title":"原理与示例","html":"\\n<ul>\\n<li><code>Map</code> 的键可以是任意值，对象键按身份比较，保留插入顺序，键不会被字符串化——这与对象字面量有本质区别。</li>\\n<li><code>Set</code> 保存唯一值，<code>NaN</code> 视为相等，<code>0</code> 与 <code>-0</code> 视为同一值。</li>\\n<li><code>WeakMap</code> 的键必须是可被弱引用的值（普通对象、函数、非注册 symbol）；不能是原始值，也不能是 <code>Symbol.for()</code> 注册的 symbol。</li>\\n<li>不可枚举是刻意设计：暴露遍历就会暴露 GC 时机，而 GC 是非确定的。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">const meta = new WeakMap()\\r\\nfunction track(el) {\\r\\n  meta.set(el, { clicks: 0 }) // el 被回收后，这条记录也自动消失\\r\\n}\\n</code></pre>\\n"},{"id":"q20-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><code>WeakMap</code> <strong>不是\\"永不泄漏\\"</strong>：value 可以强引用大对象，key 在别处强可达时也不会被回收，且完全没有容量控制。</li>\\n<li>无法遍历 <code>WeakMap</code>/<code>WeakSet</code>，因此不能实现\\"清空\\"或 LRU 淘汰——需要容量控制时用 <code>Map</code> 加显式淘汰规则。</li>\\n<li><code>Map</code>/<code>Set</code> 是强引用，拿它当缓存就必须自己管容量与 TTL，否则就是泄漏源。</li>\\n<li>序列化差异：<code>JSON.stringify(new Map())</code> 得到 <code>{}</code>，需要手动 <code>[...map]</code>；<code>structuredClone</code> 可以克隆 <code>Map</code>/<code>Set</code>。</li>\\n</ul>\\n"},{"id":"q20-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>什么时候用 <code>Map</code> 而不是普通对象？</strong> 键类型不限、需要 <code>.size</code>、增删频繁、需要稳定插入顺序，或要避开原型链键名冲突时。</li>\\n<li><strong><code>WeakRef</code>/<code>FinalizationRegistry</code> 能当缓存用吗？</strong> 不能依赖：回收与回调时机都不确定，只能作为可选优化，不能承载正确性。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":841}},{"id":21,"slug":"question-21","title":"常见前端内存泄漏来源和排查方法是什么？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q21-section-0","title":"核心答案","html":"<p>泄漏的本质是：本该被回收的对象<strong>仍然被强引用可达</strong>。前端最常见的两类成因是生命周期没有收尾，以及只增不减的缓存。</p>\\n"},{"id":"q21-section-1","title":"原理与示例","html":"\\n<p>常见来源：</p>\\n<ul>\\n<li>未卸载的全局监听（<code>window</code>/<code>document</code>）、未清理的定时器与 <code>requestAnimationFrame</code>。</li>\\n<li><code>ResizeObserver</code>/<code>IntersectionObserver</code>/<code>MutationObserver</code> 未 <code>disconnect()</code>。</li>\\n<li>事件总线、全局 store、WebSocket 的订阅未取消。</li>\\n<li>只增不减的 <code>Map</code>/数组缓存、没有容量上限的 memo。</li>\\n<li>闭包捕获大对象、detached DOM 仍被 JS 引用。</li>\\n<li>未 abort 的请求回调持有组件状态（配合竞态处理，见第 43 题）。</li>\\n</ul>\\n<p>排查闭环：</p>\\n<ol>\\n<li>定义可重复操作，例如\\"进入/退出页面 20 次\\"。</li>\\n<li>用 Performance Monitor 观察 JS heap、DOM node、listener 数的趋势。</li>\\n<li>采集两到三次 heap snapshot，用 Comparison 视图看 retained size 的增量。</li>\\n<li>从 Retaining path 反查强引用链，定位真正的持有者。</li>\\n<li>修掉 cleanup 或补上淘汰策略，用同一流程复测确认基线回落。</li>\\n</ol>\\n"},{"id":"q21-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>内存短暂上涨<strong>不能</strong>直接判定泄漏：GC 时机不确定，判据应是\\"重复同一操作后基线不回落\\"。</li>\\n<li><code>detached</code> 节点持续增长是强信号，但节点只有仍被 JS 引用时才会 detached 后继续存活。</li>\\n<li>开发模式下的 HMR、Strict Mode 双执行、浏览器扩展都会造成噪声，尽量在生产构建下测量。</li>\\n<li>只修症状（例如手动清空缓存）而不切断持有链，问题通常会换一种形式回来。</li>\\n</ul>\\n"},{"id":"q21-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>怎么区分泄漏和正常缓存？</strong> 缓存应该有容量上限或淘汰策略；重复操作后观察基线是否回到同一水平。</li>\\n<li><strong>React 里最典型的泄漏点？</strong> 订阅与定时器没有在 Effect cleanup 中释放，以及忘记 abort 的请求。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":880}},{"id":22,"slug":"question-22","title":"ESM 与 CommonJS 的核心差异是什么？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript","ESM"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q22-section-0","title":"核心答案","html":"<p>核心差异在<strong>时机</strong>与<strong>绑定语义</strong>：ESM 是静态模块语法，依赖关系在执行前即可解析，导入是 live binding；CommonJS 是运行时包装函数，<code>require</code> 同步执行，导出传递的是值。</p>\\n"},{"id":"q22-section-1","title":"原理与示例","html":"\\n<ul>\\n<li>语法与位置：ESM 的 <code>import</code>/<code>export</code> 只能出现在模块顶层，会被提升；CJS 的 <code>require</code> 可写在任意位置，按执行顺序同步加载。</li>\\n<li>绑定语义：ESM 导入的是<strong>活绑定</strong>，导出方后续更新，导入方能读到新值（但不能重新赋值）；CJS 拿到的是一次求值结果，重新赋值 <code>module.exports</code> 不会影响已经导入方持有的旧引用。</li>\\n<li>静态分析：ESM 的结构让 tree shaking 与循环依赖分析更可行；CJS 需要运行时推断，容易整包保留。</li>\\n<li>环境判定：浏览器原生只支持 ESM；Node 根据 <code>package.json</code> 的 <code>type</code>、<code>.mjs</code>/<code>.cjs</code> 扩展名与 <code>exports</code> 条件决定解析方式。</li>\\n<li>其他差异：ESM 支持 top-level await、默认严格模式、顶层 <code>this</code> 为 <code>undefined</code>；CJS 顶层 <code>this</code> 是 <code>module.exports</code>。</li>\\n</ul>\\n"},{"id":"q22-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>\\"用了 ESM 就一定能 tree shaking\\"是错的：顶层副作用、错误的 <code>sideEffects</code> 声明、整包聚合导入、依赖只发布 CJS 都会让消除失败（见第 82 题）。</li>\\n<li>循环依赖不一定立即报错：ESM 先建立绑定，访问尚未初始化的绑定会抛 TDZ 错误；CJS 常见到执行未完成时的\\"部分导出\\"。</li>\\n<li>双包发布要小心 <code>exports</code> 条件顺序与\\"同一模块被加载两次\\"，后者会导致 <code>instanceof</code> 失效、单例重复。</li>\\n<li>CJS 不能直接 <code>require</code> 纯 ESM（新版本 Node 已支持部分场景），但可以用动态 <code>import()</code> 加载。</li>\\n</ul>\\n"},{"id":"q22-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>静态 <code>import</code> 与 <code>import()</code> 的区别？</strong> 动态导入在运行时执行、返回 Promise，可用于条件加载与代码分割（见第 58 题）。</li>\\n<li><strong>怎么消除循环依赖？</strong> 重构模块边界，把共享部分抽成第三个模块，或用依赖注入替掉反向依赖。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":924}},{"id":23,"slug":"question-23","title":"防抖和节流有什么区别？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q23-section-0","title":"核心答案","html":"<p>防抖把\\"连续触发\\"收敛为静默后的一次执行；节流把执行频率限制为固定时间窗口内至多一次。前者关心\\"最后一次\\"，后者关心\\"稳定节奏\\"。</p>\\n"},{"id":"q23-section-1","title":"原理与示例","html":"\\n<ul>\\n<li><strong>防抖</strong>：每次触发都重置计时器，停止触发一段时间后才执行；适合搜索输入、表单校验、窗口尺寸变化后的重算。</li>\\n<li><strong>节流</strong>：窗口内最多执行一次，常见形态是\\"首次立即执行 + 尾部补一次\\"；适合滚动位置、resize、拖拽、埋点上报。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">function debounce(fn, wait) {\\r\\n  let timer\\r\\n  return function (...args) {\\r\\n    clearTimeout(timer)\\r\\n    timer = setTimeout(() => fn.apply(this, args), wait)\\r\\n  }\\r\\n}\\n</code></pre>\\n<p>完整实现还需要 <code>cancel</code>/<code>flush</code>、透传 <code>this</code> 与参数、以及在组件卸载时清理。</p>\\n"},{"id":"q23-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>防抖<strong>不解决竞态</strong>：慢的旧响应仍可能后到并覆盖新结果，必须额外用 <code>AbortController</code> 或请求序号（见第 43 题）。</li>\\n<li>组件卸载时必须 <code>cancel</code>，否则定时器会在组件消失后触发更新、或长期持有旧闭包造成泄漏。</li>\\n<li>leading/trailing 语义要显式定义并写测试，否则会出现\\"首次没反应\\"或\\"停止后不再更新\\"。</li>\\n<li>时间戳版节流会被系统时钟对齐影响产生边界抖动，计时器版更平滑，但要明确首次是否立即执行。</li>\\n<li>高频事件还应配合 <code>{ passive: true }</code>、批量读/写 DOM，避免回调本身造成掉帧。</li>\\n</ul>\\n"},{"id":"q23-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>搜索框应该用哪个？</strong> 通常防抖 + 请求取消；若要\\"输入即筛选本地数据\\"，节流或 <code>useDeferredValue</code> 更合适。</li>\\n<li><strong>和 <code>requestAnimationFrame</code> 的关系？</strong> rAF 天然按帧节流，适合与渲染同步的更新，但不保证固定间隔（后台标签页会暂停）。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":957}},{"id":24,"slug":"question-24","title":"事件委托为什么有效？有什么边界？","category":"javascript","type":"theory","difficulty":"advanced","tags":["JavaScript"],"estimatedMinutes":8,"promptHtml":"","answerSections":[{"id":"q24-section-0","title":"核心答案","html":"<p>事件委托利用事件冒泡：把众多子元素的监听收敛到共同祖先，再用 <code>event.target.closest()</code> 定位真正被点击的元素。它同时减少监听器数量，并天然覆盖后来插入的节点。</p>\\n"},{"id":"q24-section-1","title":"原理与示例","html":"\\n<ul>\\n<li>冒泡路径由 DOM 树决定，祖先上的监听器可以接住任意后代触发的事件。</li>\\n<li><code>event.target</code> 是实际触发元素，<code>event.currentTarget</code> 是绑定监听器的元素，委托时用前者定位、后者做范围校验。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">list.addEventListener('click', (event) => {\\r\\n  const button = event.target.closest('[data-action]')\\r\\n  if (!button || !list.contains(button)) return // 关键：确认仍在容器内\\r\\n  run(button.dataset.action)\\r\\n})\\n</code></pre>\\n"},{"id":"q24-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>并非所有事件都冒泡：<code>focus</code>/<code>blur</code>（需用 <code>focusin</code>/<code>focusout</code>）、<code>mouseenter</code>/<code>mouseleave</code>、<code>load</code> 等不冒泡，部分只能在捕获阶段处理。</li>\\n<li>Shadow DOM 会 retargeting：外部监听器看到的 <code>target</code> 是宿主元素，拿不到内部真实节点。</li>\\n<li><code>closest</code> 可能命中容器之外的祖先，必须用 <code>contains</code> 校验，否则嵌套组件会误触发。</li>\\n<li>滥用 <code>stopPropagation</code> 会破坏其他委托逻辑（模态框、埋点、埋点外的关闭行为），优先用条件判断替代。</li>\\n<li>React 17 起把事件监听挂在根容器而不是 <code>document</code> 上，跨根节点与 Portal 的冒泡行为需要注意（见第 52 题）。</li>\\n</ul>\\n"},{"id":"q24-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>为什么说委托\\"自动覆盖新节点\\"？</strong> 监听器不在子节点上，新增节点无需重新绑定。</li>\\n<li><strong>大量同类元素怎么进一步优化？</strong> 委托到容器 + 虚拟化列表；减少监听数量通常比优化回调体收益更大。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":1000}},{"id":25,"slug":"question-25","title":"为什么不可变更新对 React 很重要？","category":"javascript","type":"theory","difficulty":"intermediate","tags":["JavaScript","React"],"estimatedMinutes":5,"promptHtml":"","answerSections":[{"id":"q25-section-0","title":"核心答案","html":"<p>不可变更新不是 JavaScript 的语言要求，而是一条更新纪律。新建引用让 React 只用一次浅比较就能判断\\"变了没有\\"，同时保留旧快照，为并发渲染、撤销、调试提供前提。</p>\\n"},{"id":"q25-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">// 错误：原地修改后传回同一个引用\\r\\nuser.name = 'B'\\r\\nsetUser(user)\\r\\n\\r\\n// 正确：只复制变化路径\\r\\nsetUser(prev => ({ ...prev, name: 'B' }))\\n</code></pre>\\n<ul>\\n<li><code>useState</code> 的更新 bailout 使用 <code>Object.is</code> 比较，传入同一引用会被判定为无变化。</li>\\n<li><code>memo</code>、<code>useMemo</code>、<code>useEffect</code> 的依赖比较同样依赖引用稳定；原地修改会让\\"依赖没变所以跳过\\"的判断失效。</li>\\n<li>旧快照不可变，才使并发渲染下的可中断与重放结果可预期。</li>\\n</ul>\\n"},{"id":"q25-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>深拷贝整棵树既慢，又破坏未变化分支的引用稳定性，导致所有 memo 失效。正确做法是<strong>只复制从根到变更点的路径</strong>。</li>\\n<li>嵌套更新容易漏层（<code>{ ...prev, a: { ...prev.a, b: 1 } }</code>），可用 <code>useReducer</code> 集中管理，或用 immer 类工具生成结构共享结果。</li>\\n<li>与 class 组件的 <code>setState</code> 不同，<code>useState</code> 不做浅合并，必须自己展开。</li>\\n<li>不可变不等于\\"每层都复制\\"：未变化的分支必须沿用原引用，否则优化全部失效。</li>\\n</ul>\\n"},{"id":"q25-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>为什么并发渲染更依赖不可变？</strong> 渲染可能被中断后重放，若状态被原地修改，渲染读到的就不是同一份快照。</li>\\n<li><strong>什么时候可以接受可变？</strong> 纯计算的局部变量、<code>useRef</code> 持有的非渲染数据，以及明确不参与渲染的缓存。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":1039}},{"id":26,"slug":"question-26","title":"前端错误应该怎样分类和传播？","category":"javascript","type":"theory","difficulty":"advanced","tags":["JavaScript"],"estimatedMinutes":8,"promptHtml":"","answerSections":[{"id":"q26-section-0","title":"核心答案","html":"<p>先按\\"能否重试、谁能处理、用户该看到什么\\"分类，再为每类定义统一的传播与提示策略。核心原则：错误要么在本层被处理，要么补上下文后继续向上，绝不能静默吞掉。</p>\\n"},{"id":"q26-section-1","title":"原理与示例","html":"\\n<p>至少区分：用户输入校验错误、业务拒绝（库存不足、状态不允许）、认证与授权错误、网络与超时、限流、服务端错误、程序 bug。每类都要定义：是否重试、是否提示用户、是否保留旧数据、日志级别、恢复到哪一步。</p>\\n<pre><code class=\\"language-js\\">class ApiError extends Error {\\r\\n  constructor(message, { code, status, retryable } = {}) {\\r\\n    super(message)\\r\\n    this.name = 'ApiError'\\r\\n    this.code = code\\r\\n    this.status = status\\r\\n    this.retryable = retryable\\r\\n  }\\r\\n}\\n</code></pre>\\n<p>传播路径：网络层标准化为带 <code>code</code>/<code>status</code> 的错误对象 → 数据层（query/mutation）决定重试与缓存失效 → 路由或组件边界决定 UI → 上报层补齐上下文（release、route、requestId）。</p>\\n"},{"id":"q26-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>空的 <code>catch {}</code> 是最严重的反模式：错误被吞掉，线上只剩\\"点了没反应\\"。</li>\\n<li>当前层无法处理时要补充上下文后重新抛出，而不是就地 <code>console.log</code> 后返回一个假成功。</li>\\n<li><code>window.onerror</code> 与 <code>unhandledrejection</code> 只是最后一道观测网，拿不到业务上下文，不能替代局部处理。</li>\\n<li>Error Boundary 只覆盖渲染期错误，<strong>不捕获</strong>事件处理器与异步请求中的错误（见第 53 题）。</li>\\n<li>toast 不能作为唯一错误 UI：可能被忽略，也无法承载重试入口。</li>\\n<li>日志必须脱敏，不上报手机号、token、完整请求体等个人信息。</li>\\n</ul>\\n"},{"id":"q26-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>怎么避免同一错误刷屏？</strong> 按错误指纹（message + stack + code）聚合计数与采样上报。</li>\\n<li><strong>重试策略怎么定？</strong> 只对幂等且可恢复的类型（网络抖动、超时、5xx）做指数退避加抖动，其余直接暴露给用户。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":1079}},{"id":27,"slug":"question-27","title":"垃圾回收可以手动触发吗？WeakRef 应该用来解决普通缓存吗？","category":"javascript","type":"theory","difficulty":"advanced","tags":["JavaScript","缓存"],"estimatedMinutes":8,"promptHtml":"","answerSections":[{"id":"q27-section-0","title":"核心答案","html":"<p>业务代码不能可靠地控制 GC 时机（<code>globalThis.gc()</code> 只在特定调试模式或 Node 的 <code>--expose-gc</code> 下存在），<code>WeakRef</code> 与 <code>FinalizationRegistry</code> 的回收和回调时机同样不确定，因此不能承载正确性、关键资源释放或普通业务缓存策略。</p>\\n"},{"id":"q27-section-1","title":"原理与示例","html":"\\n<ul>\\n<li>现代引擎采用分代加增量/并发标记清除，回收时机由内存压力与空闲时间决定。</li>\\n<li><code>WeakRef.deref()</code> 可能在任何时刻返回 <code>undefined</code>；<code>FinalizationRegistry</code> 的回调甚至可能到页面卸载都没执行。</li>\\n<li>需要释放的资源（连接、订阅、文件句柄、Worker）必须显式 <code>close()</code>/<code>unsubscribe()</code>/<code>terminate()</code>。</li>\\n</ul>\\n<pre><code class=\\"language-js\\">const meta = new WeakMap() // 跟随对象生命周期，适合对象级元数据\\n</code></pre>\\n<p>普通缓存的正确做法是明确策略：容量上限 + 淘汰规则（LRU/LFU）+ TTL + 主动失效。</p>\\n"},{"id":"q27-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>\\"内存会涨\\"不等于泄漏：先确认重复操作后基线是否回落（见第 21 题）。</li>\\n<li><code>WeakRef</code> 只在同一事件循环轮次内相对稳定，跨 <code>await</code> 后不能假定仍然有效。</li>\\n<li>用 <code>FinalizationRegistry</code> 做资源清理会形成\\"有时不执行\\"的泄漏，属于设计错误。</li>\\n<li>调试手段（DevTools Memory/Performance、<code>--expose-gc</code>）只用于本地验证，不应出现在产品逻辑里。</li>\\n</ul>\\n"},{"id":"q27-section-3","title":"追问","html":"\\n<ul>\\n<li><strong><code>WeakMap</code> 能当缓存吗？</strong> 它是\\"跟随对象生命周期的关联存储\\"，没有容量与淘汰语义，只能算缓存的一个特例。</li>\\n<li><strong>什么可以依赖 GC？</strong> 只有\\"对象图变为不可达\\"这一件事；外部资源的释放一律显式处理。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":1124}},{"id":28,"slug":"question-28","title":"为什么 Array.prototype.sort 可能带来 React bug？","category":"javascript","type":"theory","difficulty":"advanced","tags":["JavaScript","React"],"estimatedMinutes":8,"promptHtml":"","answerSections":[{"id":"q28-section-0","title":"核心答案","html":"<p>因为 <code>sort</code> 是<strong>原地修改</strong>：它会直接改动 React 正在持有的 props 或 state 数组，破坏状态快照与引用比较的前提（见第 25 题）。</p>\\n"},{"id":"q28-section-1","title":"原理与示例","html":"\\n<pre><code class=\\"language-js\\">// 错误：原地排序，state 数组本身已被改动\\r\\nitems.sort(compare)\\r\\nsetItems(items) // 同一引用，更新可能被跳过\\r\\n\\r\\n// 正确：先复制再排序\\r\\nconst sorted = [...items].sort(compare)\\r\\n// 现代运行环境也可以直接用 items.toSorted(compare)\\n</code></pre>\\n<p>同类的原地方法还有 <code>reverse</code>、<code>splice</code>、<code>fill</code>、<code>copyWithin</code>、<code>push</code>/<code>pop</code>/<code>shift</code>/<code>unshift</code>。</p>\\n"},{"id":"q28-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>原地排序后再 <code>setItems(items)</code> 传入同一引用，会被 <code>Object.is</code> 判定为无变化而跳过渲染——数据变了但 UI 不刷新。</li>\\n<li>破坏快照的影响不止少渲染一次：并发渲染、撤销、<code>memo</code> 比较、其他消费者读到的旧数据都会受影响。</li>\\n<li><code>sort</code> 默认按字符串比较，数字数组必须传比较函数，否则 <code>[10, 9]</code> 的结果不符合直觉。</li>\\n<li>ES2019 起 <code>sort</code> 是稳定的，但稳定不代表可以省略比较函数，也不代表可以原地调用。</li>\\n<li><code>toSorted</code>/<code>toReversed</code>/<code>toSpliced</code> 需要较新的运行环境，兼容性不足时用 <code>[...arr]</code> 兜底。</li>\\n</ul>\\n"},{"id":"q28-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>只排序不 setState 可以吗？</strong> 不可以，会污染父组件或其他消费者共享的数据，属于跨组件副作用。</li>\\n<li><strong>团队层面怎么防？</strong> 约定\\"渲染输入只读\\"，在 code review 与 lint 层面禁止修改入参。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":1161}},{"id":86,"slug":"question-86","title":"实现 debounce，支持 cancel 和 flush","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript","debounce"],"estimatedMinutes":10,"promptHtml":"<p>实现 <code>debounce(fn, wait)</code>：连续调用只在停止 <code>wait</code> 毫秒后执行最后一次，并保留最后一次的实参与 <code>this</code>；支持 <code>cancel()</code> 与 <code>flush()</code>。</p>","answerSections":[{"id":"q86-section-0","title":"核心答案","html":"<p>思路：用闭包维护定时器与\\"最后一次调用信息\\"，把\\"是否还有待执行调用\\"编码为 <code>timer !== null</code>。</p>\\n"},{"id":"q86-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">function debounce(fn, wait) {\\r\\n  let timer = null\\r\\n  let lastArgs\\r\\n  let lastThis\\r\\n\\r\\n  function invoke() {\\r\\n    if (timer === null) return\\r\\n    clearTimeout(timer)\\r\\n    timer = null\\r\\n\\r\\n    const args = lastArgs\\r\\n    const thisArg = lastThis\\r\\n    lastArgs = lastThis = undefined\\r\\n    return fn.apply(thisArg, args)\\r\\n  }\\r\\n\\r\\n  function debounced(...args) {\\r\\n    lastArgs = args\\r\\n    lastThis = this\\r\\n\\r\\n    if (timer !== null) clearTimeout(timer)\\r\\n    timer = setTimeout(invoke, wait)\\r\\n  }\\r\\n\\r\\n  debounced.cancel = () => {\\r\\n    if (timer !== null) clearTimeout(timer)\\r\\n    timer = null\\r\\n    lastArgs = lastThis = undefined\\r\\n  }\\r\\n\\r\\n  debounced.flush = invoke\\r\\n\\r\\n  return debounced\\r\\n}\\n</code></pre>\\n"},{"id":"q86-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>必须保留<strong>最后一次</strong>实参与 <code>this</code>，否则作为对象方法或带参调用时会出错。</li>\\n<li><code>cancel()</code> 之后 <code>flush()</code> 不应再执行：<code>invoke</code> 用 <code>timer === null</code> 做守卫，并在执行后清空缓存的参数。</li>\\n<li><code>flush()</code> 的语义是\\"立即执行待执行的调用\\"，要明确它是否重置计时（本实现是执行完毕即清零）。</li>\\n<li>定时器回调本身不能抛出未捕获异常，否则会变成全局错误；必要时在 <code>invoke</code> 内部做错误边界。</li>\\n</ul>\\n"},{"id":"q86-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>如何扩展 leading/trailing/maxWait？</strong> leading 表示首次立即执行，trailing 表示尾部补一次，maxWait 用于持续触发时强制执行一次，三者组合需要额外记录\\"上次执行时间\\"。</li>\\n<li><strong>React 中怎么用？</strong> 组件卸载必须 <code>cancel()</code>，否则会在组件消失后触发更新并持有旧闭包。</li>\\n<li><strong>搜索场景够用吗？</strong> 不够：debounce 只减少调用次数，慢的旧响应仍可能覆盖新结果，需要配合取消或请求序号（见第 43 题）。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3401}},{"id":87,"slug":"question-87","title":"实现 throttle，支持 trailing","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript","throttle"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q87-section-0","title":"核心答案","html":"<p>思路：记录上次执行时间，用\\"剩余时间\\"决定是立即执行还是安排尾部执行。</p>\\n"},{"id":"q87-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">function throttle(fn, wait) {\\r\\n  let lastInvokeTime = 0\\r\\n  let timer = null\\r\\n  let lastArgs\\r\\n  let lastThis\\r\\n\\r\\n  function invoke(time) {\\r\\n    lastInvokeTime = time\\r\\n    timer = null\\r\\n\\r\\n    const args = lastArgs\\r\\n    const thisArg = lastThis\\r\\n    lastArgs = lastThis = undefined\\r\\n    return fn.apply(thisArg, args)\\r\\n  }\\r\\n\\r\\n  function throttled(...args) {\\r\\n    const now = Date.now()\\r\\n    lastArgs = args\\r\\n    lastThis = this\\r\\n\\r\\n    const remaining = wait - (now - lastInvokeTime)\\r\\n    if (remaining &#x3C;= 0 || remaining > wait) {\\r\\n      // 已过窗口，或系统时间回拨导致 remaining 异常\\r\\n      if (timer !== null) clearTimeout(timer)\\r\\n      return invoke(now)\\r\\n    }\\r\\n\\r\\n    if (timer === null) {\\r\\n      timer = setTimeout(() => invoke(Date.now()), remaining)\\r\\n    }\\r\\n  }\\r\\n\\r\\n  throttled.cancel = () => {\\r\\n    if (timer !== null) clearTimeout(timer)\\r\\n    timer = null\\r\\n    lastInvokeTime = 0\\r\\n    lastArgs = lastThis = undefined\\r\\n  }\\r\\n\\r\\n  return throttled\\r\\n}\\n</code></pre>\\n"},{"id":"q87-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>系统时间回拨会让 <code>remaining</code> 变成异常大的值，因此要额外判断 <code>remaining > wait</code>，否则可能长期不再执行。</li>\\n<li>首次调用立即执行（<code>lastInvokeTime = 0</code>），尾部调用合并在窗口结束时执行一次；这两点必须显式说明，不能含糊。</li>\\n<li><code>cancel()</code> 应同时清掉待执行的尾部调用，否则取消后仍会触发一次。</li>\\n<li>用 <code>Date.now()</code> 的节流是按挂钟时间对齐，页面切到后台再回来可能出现\\"补执行\\"；如果只是为了让动画跟帧同步，直接用 <code>requestAnimationFrame</code>。</li>\\n</ul>\\n"},{"id":"q87-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>节流和 <code>requestAnimationFrame</code> 怎么选？</strong> 需要固定时间间隔（限流上报、滚动采样）用节流；与渲染帧对齐用 rAF。</li>\\n<li><strong>怎样避免高频回调本身造成卡顿？</strong> 回调里批量读、批量写 DOM，并给监听器加 <code>{ passive: true }</code>（见第 77 题）。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3468}},{"id":88,"slug":"question-88","title":"实现并发限制器 mapLimit","category":"javascript","type":"coding","difficulty":"advanced","tags":["JavaScript","并发"],"estimatedMinutes":10,"promptHtml":"<p>给定输入数组、最大并发数与异步 mapper，按输入顺序返回结果。</p>","answerSections":[{"id":"q88-section-0","title":"核心答案","html":"<p>思路：启动 <code>limit</code> 个\\"工人\\"，每个工人循环从共享游标领取下一个下标，天然实现\\"完成即补位\\"。</p>\\n"},{"id":"q88-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">async function mapLimit(items, limit, mapper) {\\r\\n  if (!Number.isInteger(limit) || limit &#x3C;= 0) {\\r\\n    throw new RangeError('limit must be a positive integer')\\r\\n  }\\r\\n\\r\\n  const results = new Array(items.length)\\r\\n  let nextIndex = 0\\r\\n\\r\\n  async function worker() {\\r\\n    while (true) {\\r\\n      const index = nextIndex++\\r\\n      if (index >= items.length) return\\r\\n      results[index] = await mapper(items[index], index)\\r\\n    }\\r\\n  }\\r\\n\\r\\n  const workerCount = Math.min(limit, items.length)\\r\\n  await Promise.all(Array.from({ length: workerCount }, worker))\\r\\n  return results\\r\\n}\\n</code></pre>\\n"},{"id":"q88-section-2","title":"复杂度与取舍","html":"\\n<ul>\\n<li>时间复杂度由 mapper 的工作量决定；调度额外空间 O(n)（结果数组），同时最多有 <code>limit</code> 个任务在途。</li>\\n<li>结果按输入顺序写回下标位置，与完成顺序无关——这是与\\"完成即 push\\"的关键区别。</li>\\n<li>因为 <code>nextIndex++</code> 是同步操作，单线程下不需要额外加锁，不会出现两个工人领到同一下标。</li>\\n</ul>\\n"},{"id":"q88-section-3","title":"边界与易错点","html":"\\n<ul>\\n<li>任一任务拒绝时 <code>Promise.all</code> 会快速拒绝，但<strong>已启动的任务不会自动取消</strong>；需要取消时必须传入 <code>AbortSignal</code>。</li>\\n<li><code>limit</code> 大于数组长度时只启动 <code>items.length</code> 个工人，避免创建空转任务。</li>\\n<li>空数组应当直接返回 <code>[]</code>，不要因为 <code>workerCount</code> 为 0 而产生 <code>Promise.all([])</code> 的假成功歧义。</li>\\n<li>生产实现通常还要支持：错误策略（fail-fast 与 all-settled）、重试、进度回调与总超时。</li>\\n</ul>\\n"},{"id":"q88-section-4","title":"追问","html":"\\n<ul>\\n<li><strong>为什么不用\\"分批 <code>Promise.all</code>\\"？</strong> 分批会让慢任务拖住整批，利用率低于流水线式领取。</li>\\n<li><strong>怎么加速？</strong> 提高 <code>limit</code> 之前先确认瓶颈在下游（接口限流、数据库连接数），盲目提高并发只会制造更多失败。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3540}},{"id":89,"slug":"question-89","title":"手写 Promise.all","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript","Promise"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q89-section-0","title":"核心答案","html":"<p>思路：收集计数 + 按索引写入结果，先到的不影响顺序，全部完成才 resolve。</p>\\n"},{"id":"q89-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">function promiseAll(iterable) {\\r\\n  return new Promise((resolve, reject) => {\\r\\n    const values = Array.from(iterable)\\r\\n    if (values.length === 0) {\\r\\n      resolve([])\\r\\n      return\\r\\n    }\\r\\n\\r\\n    const results = new Array(values.length)\\r\\n    let fulfilledCount = 0\\r\\n\\r\\n    values.forEach((value, index) => {\\r\\n      Promise.resolve(value).then(\\r\\n        (result) => {\\r\\n          results[index] = result\\r\\n          fulfilledCount += 1\\r\\n          if (fulfilledCount === values.length) resolve(results)\\r\\n        },\\r\\n        reject,\\r\\n      )\\r\\n    })\\r\\n  })\\r\\n}\\n</code></pre>\\n"},{"id":"q89-section-2","title":"复杂度与取舍","html":"\\n<ul>\\n<li>时间由最慢的任务决定，空间 O(n)。</li>\\n<li>用计数器而不是 <code>results.length</code> 判断完成，因为\\"先失败\\"或稀疏赋值都会让长度不可靠。</li>\\n</ul>\\n"},{"id":"q89-section-3","title":"评分点","html":"\\n<ul>\\n<li>接受 iterable，并把普通值与 thenable 都统一交给 <code>Promise.resolve</code> 处理。</li>\\n<li>空输入立即 fulfilled 为 <code>[]</code>。</li>\\n<li>结果<strong>按输入顺序</strong>排列，而不是完成顺序。</li>\\n<li>首个 rejection 让整体拒绝；其余底层任务不会被取消（真正取消要 <code>AbortController</code>，见第 17 题）。</li>\\n<li>明确说明这是行为近似实现，不等价于完整复刻 ECMAScript 内部算法与 subclass/species 语义。</li>\\n</ul>\\n"},{"id":"q89-section-4","title":"追问","html":"\\n<ul>\\n<li><strong>为什么不能用 <code>results.push</code>？</strong> 完成顺序与输入顺序不一致，结果会错位。</li>\\n<li><strong><code>Promise.all</code> 会等到所有任务结束吗？</strong> 不会，首个 rejection 后立即拒绝，但其他任务仍在后台运行。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3598}},{"id":90,"slug":"question-90","title":"实现 EventEmitter","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q90-section-0","title":"核心答案","html":"<p>思路：用 <code>Map&#x3C;type, Set&#x3C;listener>></code> 存储，<code>emit</code> 遍历快照以防遍历期间被修改。</p>\\n"},{"id":"q90-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">class EventEmitter {\\r\\n  #events = new Map()\\r\\n\\r\\n  on(type, listener) {\\r\\n    const listeners = this.#events.get(type) ?? new Set()\\r\\n    listeners.add(listener)\\r\\n    this.#events.set(type, listeners)\\r\\n    return () => this.off(type, listener)\\r\\n  }\\r\\n\\r\\n  once(type, listener) {\\r\\n    const off = this.on(type, (...args) => {\\r\\n      off()\\r\\n      listener(...args)\\r\\n    })\\r\\n    return off\\r\\n  }\\r\\n\\r\\n  off(type, listener) {\\r\\n    const listeners = this.#events.get(type)\\r\\n    if (!listeners) return\\r\\n    listeners.delete(listener)\\r\\n    if (listeners.size === 0) this.#events.delete(type)\\r\\n  }\\r\\n\\r\\n  emit(type, ...args) {\\r\\n    const listeners = this.#events.get(type)\\r\\n    if (!listeners) return false\\r\\n\\r\\n    // 快照：避免监听器在 emit 过程中增删导致本轮遍历异常\\r\\n    for (const listener of [...listeners]) {\\r\\n      listener(...args)\\r\\n    }\\r\\n    return true\\r\\n  }\\r\\n}\\n</code></pre>\\n"},{"id":"q90-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>必须对监听器集合做<strong>快照</strong>：直接遍历原集合时，监听器内部调用 <code>off</code> 可能跳过元素或产生意外。</li>\\n<li><code>off</code> 后若集合为空应删除该 key，否则长时间运行会残留大量空集合（见第 21 题）。</li>\\n<li>用 <code>Set</code> 意味着同一函数重复注册会被去重；如果业务需要\\"注册两次触发两次\\"，应改用数组并实现按引用移除。</li>\\n<li><code>once</code> 必须在调用前先移除，否则监听器内部再次 emit 会递归触发。</li>\\n<li>私有字段 <code>#events</code> 防止外部直接改写内部结构。</li>\\n</ul>\\n"},{"id":"q90-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>监听器抛错怎么办？</strong> 需要明确策略：抛出中断后续（默认）还是逐个 <code>try/catch</code> 并汇总上报。生产库通常选择隔离错误并上报，避免一个订阅者拖垮所有订阅者。</li>\\n<li><strong>要不要支持 async 监听器？</strong> 若支持，要明确 <code>emit</code> 是否等待所有监听器完成；混用\\"部分 await、部分不 await\\"是最容易出错的设计。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3657}},{"id":91,"slug":"question-91","title":"实现 O(1) 的 LRU Cache","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript","LRU"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q91-section-0","title":"核心答案","html":"<p>思路：<code>Map</code> 保留插入顺序，用\\"删除后重新插入\\"把 key 标记为最近使用，<code>keys().next()</code> 得到最久未使用的 key。</p>\\n"},{"id":"q91-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">class LRUCache {\\r\\n  constructor(capacity) {\\r\\n    if (!Number.isInteger(capacity) || capacity &#x3C;= 0) {\\r\\n      throw new RangeError('capacity must be positive')\\r\\n    }\\r\\n    this.capacity = capacity\\r\\n    this.cache = new Map()\\r\\n  }\\r\\n\\r\\n  get(key) {\\r\\n    if (!this.cache.has(key)) return undefined\\r\\n    const value = this.cache.get(key)\\r\\n    this.cache.delete(key)\\r\\n    this.cache.set(key, value) // 视为最近使用\\r\\n    return value\\r\\n  }\\r\\n\\r\\n  set(key, value) {\\r\\n    if (this.cache.has(key)) this.cache.delete(key)\\r\\n    this.cache.set(key, value)\\r\\n\\r\\n    if (this.cache.size > this.capacity) {\\r\\n      const oldestKey = this.cache.keys().next().value\\r\\n      this.cache.delete(oldestKey)\\r\\n    }\\r\\n    return this\\r\\n  }\\r\\n}\\n</code></pre>\\n"},{"id":"q91-section-2","title":"复杂度与取舍","html":"\\n<ul>\\n<li><code>get</code>/<code>set</code> 均为均摊 O(1)：<code>Map</code> 的删除与插入、<code>keys().next()</code> 取首元素都是常数级。</li>\\n<li>依赖 <code>Map</code> 的插入顺序保证，并非所有语言的字典都有这个性质。</li>\\n<li>相比\\"双向链表 + 哈希表\\"的经典实现，这版代码量小很多，代价是依赖引擎对 <code>Map</code> 顺序的实现。</li>\\n</ul>\\n"},{"id":"q91-section-3","title":"边界与易错点","html":"\\n<ul>\\n<li>缓存值为 <code>undefined</code> 时，<code>get</code> 返回 <code>undefined</code> 无法区分\\"未命中\\"与\\"命中但值为 undefined\\"；应额外提供 <code>has</code> 或返回标记对象。</li>\\n<li><code>capacity</code> 必须校验为<strong>正</strong>整数，否则 <code>set</code> 会立刻把刚写入的值淘汰掉。</li>\\n<li>生产缓存还要考虑 TTL、按内存大小而非条数淘汰、统计命中率、以及并发请求去重（single-flight，见第 111 题）。</li>\\n<li><code>set</code> 返回 <code>this</code> 便于链式调用；如果 API 约定不返回内容，要显式说明，避免调用方误用返回值。</li>\\n</ul>\\n"},{"id":"q91-section-4","title":"追问","html":"\\n<ul>\\n<li><strong>为什么不用对象当缓存？</strong> 对象键会被字符串化，无法区分 <code>1</code> 与 <code>'1'</code>，也没有插入顺序语义。</li>\\n<li><strong>怎么加 TTL？</strong> 存 <code>{ value, expiresAt }</code>，读取时判断过期；注意过期清理与容量淘汰的顺序要一致。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3724}},{"id":92,"slug":"question-92","title":"实现支持循环引用的 deepClone","category":"javascript","type":"coding","difficulty":"advanced","tags":["JavaScript"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q92-section-0","title":"核心答案","html":"<p>思路：用 <code>WeakMap</code> 记录\\"原对象 → 副本\\"的映射，既解决循环引用，也保证同一对象在图里只被复制一次。</p>\\n"},{"id":"q92-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">function deepClone(value, seen = new WeakMap()) {\\r\\n  if (typeof value !== 'object' || value === null) return value\\r\\n  if (seen.has(value)) return seen.get(value)\\r\\n\\r\\n  if (value instanceof Date) return new Date(value.getTime())\\r\\n  if (value instanceof RegExp) return new RegExp(value.source, value.flags)\\r\\n\\r\\n  if (value instanceof Map) {\\r\\n    const result = new Map()\\r\\n    seen.set(value, result)\\r\\n    for (const [k, v] of value) result.set(deepClone(k, seen), deepClone(v, seen))\\r\\n    return result\\r\\n  }\\r\\n\\r\\n  if (value instanceof Set) {\\r\\n    const result = new Set()\\r\\n    seen.set(value, result)\\r\\n    for (const item of value) result.add(deepClone(item, seen))\\r\\n    return result\\r\\n  }\\r\\n\\r\\n  const result = Array.isArray(value) ? [] : Object.create(Object.getPrototypeOf(value))\\r\\n  seen.set(value, result)\\r\\n\\r\\n  for (const key of Reflect.ownKeys(value)) {\\r\\n    const descriptor = Object.getOwnPropertyDescriptor(value, key)\\r\\n    if ('value' in descriptor) descriptor.value = deepClone(descriptor.value, seen)\\r\\n    Object.defineProperty(result, key, descriptor)\\r\\n  }\\r\\n\\r\\n  return result\\r\\n}\\n</code></pre>\\n"},{"id":"q92-section-2","title":"复杂度与取舍","html":"\\n<ul>\\n<li>时间与空间均为 O(n)（n 为可达属性数）；<code>WeakMap</code> 不阻止原对象被回收。</li>\\n<li>用属性描述符复制而非简单赋值，因此能保留 getter/setter、不可枚举与 symbol 键；代价是复杂度上升。</li>\\n<li>用 <code>Object.create(Object.getPrototypeOf(value))</code> 保留原型，比 <code>{...value}</code> 更接近原语义。</li>\\n</ul>\\n"},{"id":"q92-section-3","title":"边界与易错点","html":"\\n<ul>\\n<li>仍未覆盖：<code>WeakMap</code>/<code>WeakSet</code>、<code>Promise</code>、<code>Error</code> 的内部槽、<code>ArrayBuffer</code> 与视图、DOM 节点、函数闭包。</li>\\n<li>自定义类若依赖构造函数参数或私有字段（<code>#x</code>），简单复制属性无法还原，需要类自己提供克隆方法。</li>\\n<li><code>seen</code> 必须在<strong>递归之前</strong>写入，否则循环引用会无限递归或栈溢出。</li>\\n<li>面试的加分回答是：先问数据范围与是否需要保留原型；平台支持时优先评估 <code>structuredClone</code>（见第 12 题）；React 状态更新通常不该深拷贝整棵树（见第 25 题）。</li>\\n</ul>\\n"},{"id":"q92-section-4","title":"追问","html":"\\n<ul>\\n<li><strong>为什么用 <code>WeakMap</code> 而不是 <code>Map</code>？</strong> 克隆结束后映射就应释放，弱引用不会阻止原对象回收。</li>\\n<li><strong><code>structuredClone</code> 能替代吗？</strong> 大多数数据场景可以，但它不支持函数、DOM 与自定义原型语义。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3788}},{"id":93,"slug":"question-93","title":"实现带 AbortSignal 和指数退避的 retry","category":"javascript","type":"coding","difficulty":"advanced","tags":["JavaScript"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q93-section-0","title":"核心答案","html":"<p>思路：把\\"可中断的等待\\"和\\"重试决策\\"拆成两件事——<code>sleep</code> 负责响应 <code>AbortSignal</code>，<code>retry</code> 负责次数、退避与是否值得重试。</p>\\n"},{"id":"q93-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">function sleep(ms, signal) {\\r\\n  return new Promise((resolve, reject) => {\\r\\n    if (signal?.aborted) {\\r\\n      reject(signal.reason ?? new DOMException('Aborted', 'AbortError'))\\r\\n      return\\r\\n    }\\r\\n\\r\\n    const id = setTimeout(resolve, ms)\\r\\n    signal?.addEventListener(\\r\\n      'abort',\\r\\n      () => {\\r\\n        clearTimeout(id)\\r\\n        reject(signal.reason ?? new DOMException('Aborted', 'AbortError'))\\r\\n      },\\r\\n      { once: true },\\r\\n    )\\r\\n  })\\r\\n}\\r\\n\\r\\nasync function retry(task, options = {}) {\\r\\n  const { retries = 3, baseDelay = 200, signal, shouldRetry = () => true } = options\\r\\n\\r\\n  let lastError\\r\\n  for (let attempt = 0; attempt &#x3C;= retries; attempt++) {\\r\\n    if (signal?.aborted) throw signal.reason\\r\\n\\r\\n    try {\\r\\n      return await task({ attempt, signal })\\r\\n    } catch (error) {\\r\\n      lastError = error\\r\\n      if (attempt === retries || !shouldRetry(error)) throw error\\r\\n\\r\\n      const exponential = baseDelay * 2 ** attempt\\r\\n      const jitter = Math.random() * exponential * 0.2\\r\\n      await sleep(exponential + jitter, signal)\\r\\n    }\\r\\n  }\\r\\n\\r\\n  throw lastError\\r\\n}\\n</code></pre>\\n"},{"id":"q93-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li><strong>不能默认重试所有失败</strong>：认证失败、参数错误、业务拒绝重试没有意义；只有网络抖动、超时、5xx 这类才值得。</li>\\n<li>非幂等 mutation（下单、支付）重试可能重复写入，必须配合服务端幂等键（见第 72 题）。</li>\\n<li>退避要加<strong>抖动</strong>，否则大量客户端会在同一时刻重试，形成惊群。</li>\\n<li>服务端返回 429/503 时优先参考 <code>Retry-After</code>，不要用固定策略覆盖服务端意图。</li>\\n<li><code>sleep</code> 必须响应 abort 并清理定时器，否则取消后仍会占着计时器资源。</li>\\n<li>需要设置<strong>总超时上限</strong>，避免\\"每次重试都很快失败但次数很多\\"导致长时间挂起。</li>\\n</ul>\\n"},{"id":"q93-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>和 <code>AbortSignal.timeout</code> 怎么配合？</strong> 它可以给单次请求设超时，外层 retry 用另一个 signal 控制整体取消，两者可以组合。</li>\\n<li><strong>怎样观测重试效果？</strong> 记录 attempt 次数与最终结果分布，重试率异常升高通常意味着下游已劣化。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3856}},{"id":94,"slug":"question-94","title":"实现\\"只采用最新请求结果\\"","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript","请求"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q94-section-0","title":"核心答案","html":"<p>思路：双重保险——用 <code>AbortController</code> 取消旧任务释放资源，用单调递增的 <code>version</code> 作为\\"谁是最新\\"的最终判据。</p>\\n"},{"id":"q94-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">function createLatestRunner() {\\r\\n  let version = 0\\r\\n  let controller = null\\r\\n\\r\\n  return async function run(task) {\\r\\n    const currentVersion = ++version\\r\\n    controller?.abort()\\r\\n    controller = new AbortController()\\r\\n\\r\\n    try {\\r\\n      const value = await task(controller.signal)\\r\\n      if (currentVersion !== version) return { status: 'stale' }\\r\\n      return { status: 'success', value }\\r\\n    } catch (error) {\\r\\n      if (controller.signal.aborted || currentVersion !== version) {\\r\\n        return { status: 'stale' }\\r\\n      }\\r\\n      return { status: 'error', error }\\r\\n    }\\r\\n  }\\r\\n}\\n</code></pre>\\n"},{"id":"q94-section-2","title":"复杂度与取舍","html":"\\n<ul>\\n<li>每次调用新增一次 <code>AbortController</code>，空间开销可忽略；判定为 O(1)。</li>\\n<li>返回值显式区分 <code>success</code>/<code>error</code>/<code>stale</code>，调用方不需要靠\\"结果是否为空\\"猜状态。</li>\\n</ul>\\n"},{"id":"q94-section-3","title":"评分点","html":"\\n<ul>\\n<li>取消旧任务只能<strong>尽力而为</strong>：底层任务可能不支持取消，所以必须有 version 判据兜底。</li>\\n<li><code>catch</code> 里要区分\\"被自己取消\\"与\\"真实错误\\"，否则会把正常取消当成错误上报（见第 43 题）。</li>\\n<li>不要用共享的 <code>controller</code> 变量判断当前调用是否被取消——它在连续调用中已被替换成新的实例；本实现用 <code>currentVersion</code> 做最终判断正是为了避免这个坑。</li>\\n<li>返回值约定要与调用方对齐：是抛错、返回 <code>null</code>，还是返回状态对象。</li>\\n</ul>\\n"},{"id":"q94-section-4","title":"追问","html":"\\n<ul>\\n<li><strong>和 Effect 里的竞态处理有何不同？</strong> 这里把竞态封装成可复用工具，Effect 版本还需要在 cleanup 中触发取消。</li>\\n<li><strong>服务端渲染场景会怎样？</strong> 没有 <code>AbortController</code> 语义时退化为纯 version 判断，因此这条路径必须保留。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3928}},{"id":95,"slug":"question-95","title":"数组转树与树转数组","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q95-section-0","title":"核心答案","html":"<p>思路：数组转树用\\"一次建索引 + 一次挂接\\"；树转数组用显式栈做深度优先遍历，避免递归深度限制。</p>\\n"},{"id":"q95-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">function arrayToTree(items) {\\r\\n  const nodes = new Map(items.map((item) => [item.id, { ...item, children: [] }]))\\r\\n  const roots = []\\r\\n\\r\\n  for (const item of items) {\\r\\n    const node = nodes.get(item.id)\\r\\n\\r\\n    if (item.parentId == null) {\\r\\n      roots.push(node)\\r\\n      continue\\r\\n    }\\r\\n\\r\\n    const parent = nodes.get(item.parentId)\\r\\n    if (!parent) throw new Error(\`Missing parent: \${item.parentId}\`)\\r\\n    parent.children.push(node)\\r\\n  }\\r\\n\\r\\n  return roots\\r\\n}\\r\\n\\r\\nfunction flattenTree(roots) {\\r\\n  const result = []\\r\\n  const stack = [...roots].reverse()\\r\\n\\r\\n  while (stack.length) {\\r\\n    const node = stack.pop()\\r\\n    const { children = [], ...rest } = node\\r\\n    result.push(rest)\\r\\n\\r\\n    for (let i = children.length - 1; i >= 0; i--) {\\r\\n      stack.push(children[i])\\r\\n    }\\r\\n  }\\r\\n\\r\\n  return result\\r\\n}\\n</code></pre>\\n"},{"id":"q95-section-2","title":"复杂度与取舍","html":"\\n<ul>\\n<li>两者时间都是 O(n)，额外空间 O(n)（索引表/栈）。</li>\\n<li>用 Map 建索引避免每次查找都遍历数组，否则会退化成 O(n²)。</li>\\n<li>用显式栈而非递归，避免深层树导致栈溢出，也便于改成\\"带层级信息\\"的输出。</li>\\n</ul>\\n"},{"id":"q95-section-3","title":"边界与易错点","html":"\\n<ul>\\n<li>必须先澄清：id 是否唯一、parentId 缺失时抛错还是忽略、是否允许多个根、children 的输出顺序。</li>\\n<li>基础实现<strong>无法检测环</strong>：存在环时会得到\\"没有根\\"或丢节点，需要额外的访问状态（如三色标记）才能报错。</li>\\n<li>缺失 parent 时直接抛错比静默忽略更好，否则会悄悄丢数据；确实需要容错时应显式记录丢弃项。</li>\\n<li><code>flattenTree</code> 用解构去掉 <code>children</code> 生成新对象；如果调用方需要引用复用，先说明语义再决定是否浅拷贝。</li>\\n<li>大树的原地挂接会共享输入对象属性，若 <code>items</code> 后续被修改，树的节点也会变化。</li>\\n</ul>\\n"},{"id":"q95-section-4","title":"追问","html":"\\n<ul>\\n<li><strong>需要保持原数组顺序吗？</strong> 需要时用 <code>children</code> 的顺序对齐输入顺序，这要求输入本身有序。</li>\\n<li><strong>超大树怎么优化？</strong> 避免创建中间对象，直接复用节点并按需缓存父级引用；必要时改为流式处理。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":3984}},{"id":96,"slug":"question-96","title":"实现 compose","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q96-section-0","title":"核心答案","html":"<p>思路：<code>compose(f, g, h)(x)</code> 等价于 <code>f(g(h(x)))</code>，因此用 <code>reduceRight</code> 从右向左依次喂入结果。</p>\\n"},{"id":"q96-section-1","title":"参考实现","html":"\\n<pre><code class=\\"language-js\\">const compose = (...functions) => (input) =>\\r\\n  functions.reduceRight((value, fn) => fn(value), input)\\r\\n\\r\\nconst trim = (value) => value.trim()\\r\\nconst lower = (value) => value.toLowerCase()\\r\\nconst slug = (value) => value.replaceAll(/\\\\s+/g, '-')\\r\\n\\r\\ncompose(slug, lower, trim)('  Hello World  ') // 'hello-world'\\n</code></pre>\\n"},{"id":"q96-section-2","title":"边界与易错点","html":"\\n<ul>\\n<li>这版只处理<strong>单参数同步</strong>函数，也不做空数组校验：<code>compose()(x)</code> 会直接返回 <code>x</code>，需要明确这是否符合预期。</li>\\n<li><code>pipe</code> 与 <code>compose</code> 方向相反（从左到右），命名必须在项目里统一，否则极易读错顺序。</li>\\n<li>若第一个函数需要多个参数，签名要重新设计（例如返回 <code>(...args) => ...</code>）。</li>\\n<li>异步场景需要专门的 <code>composeAsync</code> 并串行 await，混用同步与异步会让错误传播难以推理。</li>\\n<li>TypeScript 下要给出重载或可变元组类型，否则类型会退化为 <code>any → any</code>，失去推导价值。</li>\\n<li>不要为了函数式术语牺牲可读性：两三个清晰的局部变量有时比一层 compose 更好维护。</li>\\n</ul>\\n"},{"id":"q96-section-3","title":"追问","html":"\\n<ul>\\n<li><strong>为什么用 <code>reduceRight</code>？</strong> 它天然对应\\"从最内层开始求值\\"的顺序，避免手写反向循环。</li>\\n<li><strong>中间件是同一种模式吗？</strong> 思想相近，但中间件还涉及 <code>next</code> 控制流与异步，不能简单套用这个实现。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":4057}},{"id":97,"slug":"question-97","title":"实现并解释大数金额相加","category":"javascript","type":"coding","difficulty":"intermediate","tags":["JavaScript"],"estimatedMinutes":10,"promptHtml":"","answerSections":[{"id":"q97-section-0","title":"核心答案","html":"<p>金额不能用二进制浮点小数直接运算：</p>\\n<pre><code class=\\"language-js\\">0.1 + 0.2 // 0.30000000000000004\\r\\n0.1 + 0.2 === 0.3 // false\\n</code></pre>\\n<p>正确做法是<strong>以最小单位整数表示金额</strong>（分），在安全整数范围内用 <code>Number</code>，超出范围或需要更大精度用 <code>BigInt</code>：</p>\\n<pre><code class=\\"language-js\\">function addCents(a, b) {\\r\\n  return BigInt(a) + BigInt(b)\\r\\n}\\r\\n\\r\\n// 展示时再转成小数文本，避免中途回到浮点\\r\\nfunction formatCents(cents, currency = 'CNY') {\\r\\n  return new Intl.NumberFormat('zh-CN', { style: 'currency', currency }).format(Number(cents) / 100)\\r\\n}\\n</code></pre>\\n"},{"id":"q97-section-1","title":"边界与易错点","html":"\\n<ul>\\n<li><code>Number.MAX_SAFE_INTEGER</code> 是 <code>2^53 - 1</code>，约 9007 万亿分（约 900 亿元）；超过后整数运算会丢精度，此时必须用 <code>BigInt</code>。</li>\\n<li><code>BigInt</code> 不能与 <code>Number</code> 混算（<code>1n + 1</code> 抛 <code>TypeError</code>），<code>JSON.stringify</code> 也会抛错，需要自己序列化成字符串。</li>\\n<li>舍入规则必须<strong>先定义再实现</strong>：四舍五入、银行家舍入（round half to even）、截断在不同业务里结果不同，且税务/计息场景有明确要求。</li>\\n<li>乘法与除法是精度最容易失控的地方：应保持\\"整数运算 + 最后一次舍入\\"，中间不要过早转成小数。</li>\\n<li>如果使用第三方 decimal 方案，要确认版本维护状态、序列化方式与团队其他服务的对齐情况，避免前后端对不齐。</li>\\n</ul>\\n"},{"id":"q97-section-2","title":"追问","html":"\\n<ul>\\n<li><strong>为什么不直接用 <code>toFixed(2)</code>？</strong> 它是对浮点结果做格式化，误差已经发生；只能用于展示末端的兜底，不能作为计算手段。</li>\\n<li><strong>前后端金额怎么传？</strong> 统一传最小单位整数或字符串，避免 JSON 数字在不同语言间被解析成浮点。</li>\\n<li><strong>多币种怎么办？</strong> 币种精度不同（JPY 无小数位，部分币种三位），必须把币种与精度一起建模，不能硬编码两位。</li>\\n</ul>"}],"source":{"file":"fe/JavaScript_React原理与实战模拟面试题库.md","line":4097}}]`);export{e as default};